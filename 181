local function T()
	if type(getgenv) == "function" then
		return getgenv();
	end;
	return _G;
end;
do
	local l = T();
	local O = tostring(l.SALT_KEY or "");
	if O == "" then
		error("This script is Salt Protected. Set SALT_KEY before running it.", 0);
	end;
end;
prodordie = {
		_font_face = Drawing.Fonts.SystemBold,
		_font_size = 13,
		_pool = {
			Square = {},
			Text = {},
			Triangle = {},
			Circle = {},
			Line = {},
		},
		_pool_state = {
			Square = {},
			Text = {},
			Triangle = {},
			Circle = {},
			Line = {},
		},
		_pIdx = {
			Square = 0,
			Text = 0,
			Triangle = 0,
			Circle = 0,
			Line = 0,
		},
		_tree = {},
		_tab_order = {},
		_menu_open = true,
		_watermark_enabled = true,
		_notifications = {},
		_notifications_spawned = 0,
		_open_tab = nil,
		_next_tab = nil,
		_tab_changing = false,
		_tab_fade = 1,
		_search_query = "",
		_search_typing = false,
		_search_w_anim = 22,
		_inputs = {
			m1 = { id = 1, held = false, click = false },
			m2 = { id = 2, held = false, click = false },
			mb = { id = 4, held = false, click = false },
			backspace = { id = 8, held = false, click = false },
			tab = { id = 9, held = false, click = false },
			enter = { id = 13, held = false, click = false },
			shift = { id = 16, held = false, click = false },
			ctrl = { id = 17, held = false, click = false },
			alt = { id = 18, held = false, click = false },
			pause = { id = 19, held = false, click = false },
			capslock = { id = 20, held = false, click = false },
			esc = { id = 27, held = false, click = false },
			space = { id = 32, held = false, click = false },
			pageup = { id = 33, held = false, click = false },
			pagedown = { id = 34, held = false, click = false },
			["end"] = { id = 35, held = false, click = false },
			home = { id = 36, held = false, click = false },
			left = { id = 37, held = false, click = false },
			up = { id = 38, held = false, click = false },
			right = { id = 39, held = false, click = false },
			down = { id = 40, held = false, click = false },
			insert = { id = 45, held = false, click = false },
			delete = { id = 46, held = false, click = false },
			["0"] = { id = 48, held = false, click = false },
			["1"] = { id = 49, held = false, click = false },
			["2"] = { id = 50, held = false, click = false },
			["3"] = { id = 51, held = false, click = false },
			["4"] = { id = 52, held = false, click = false },
			["5"] = { id = 53, held = false, click = false },
			["6"] = { id = 54, held = false, click = false },
			["7"] = { id = 55, held = false, click = false },
			["8"] = { id = 56, held = false, click = false },
			["9"] = { id = 57, held = false, click = false },
			a = { id = 65, held = false, click = false },
			b = { id = 66, held = false, click = false },
			c = { id = 67, held = false, click = false },
			d = { id = 68, held = false, click = false },
			e = { id = 69, held = false, click = false },
			f = { id = 70, held = false, click = false },
			g = { id = 71, held = false, click = false },
			h = { id = 72, held = false, click = false },
			i = { id = 73, held = false, click = false },
			j = { id = 74, held = false, click = false },
			k = { id = 75, held = false, click = false },
			l = { id = 76, held = false, click = false },
			m = { id = 77, held = false, click = false },
			n = { id = 78, held = false, click = false },
			o = { id = 79, held = false, click = false },
			p = { id = 80, held = false, click = false },
			q = { id = 81, held = false, click = false },
			r = { id = 82, held = false, click = false },
			s = { id = 83, held = false, click = false },
			t = { id = 84, held = false, click = false },
			u = { id = 85, held = false, click = false },
			v = { id = 86, held = false, click = false },
			w = { id = 87, held = false, click = false },
			x = { id = 88, held = false, click = false },
			y = { id = 89, held = false, click = false },
			z = { id = 90, held = false, click = false },
			f1 = { id = 112, held = false, click = false },
			f2 = { id = 113, held = false, click = false },
			f3 = { id = 114, held = false, click = false },
			f4 = { id = 115, held = false, click = false },
			f5 = { id = 116, held = false, click = false },
			f6 = { id = 117, held = false, click = false },
			f7 = { id = 118, held = false, click = false },
			f8 = { id = 119, held = false, click = false },
			f9 = { id = 120, held = false, click = false },
			f10 = { id = 121, held = false, click = false },
			f11 = { id = 122, held = false, click = false },
			f12 = { id = 123, held = false, click = false },
		},
		_slider_drag = nil,
		_dd_drag = nil,
		_slider_disp = {},
		_menu_drag = nil,
		_input_ctx = nil,
		_key_repeat = {},
		_overwrite_menu_key = false,
		_menu_key = "f5",
		_active_dropdown = nil,
		_dropdown_scroll = 0,
		_dropdown_scroll_v = 0,
		_active_colorpicker = nil,
		_copied_color = nil,
		_activities = {},
		_toggle_anim = {},
		_dd_arrow_anim = {},
		_frame_tooltip = nil,
		_menu_scale = 0,
		_current_h = 500,
		_KB_W = 52,
		_unloaded = false,
		_unloading_sequence = false,
		_unload_phase = "sections",
		_unload_section_alpha = 1,
		_last_step_time = 0,
		_real_m1_click = false,
		_sec_scroll = {},
		_sec_slide = {},
		_item_vis = {},
		_tab_scroll = {},
		_config_folder = "prodordie",
		_config_name = "default",
		_config_refs = {},
		_config_callbacks = {},
		_autosave_dirty = false,
		_autosave_timer = 0,
		_autosave_delay = 3,
		_autosave_last = 0,
		_config_list = {},
		_autoload_requested = false,
		_boot_loaded = false,
		title = "prodordie",
		_custom_title_enabled = false,
		_custom_title = "prodordie",
		w = 420,
		h = 500,
		x = 20,
		y = 100,
		_padding = 10,
		_tab_h = 36,
		_corner = 10,
		_theme_name = "Midnight",
		_themes = {
			Light = {
				accent = Color3.fromRGB(0, 122, 255),
				unsafe = Color3.fromRGB(255, 59, 48),
				body = Color3.fromRGB(236, 236, 236),
				text = Color3.fromRGB(28, 28, 28),
				subtext = Color3.fromRGB(142, 142, 147),
				border1 = Color3.fromRGB(198, 198, 203),
				border0 = Color3.fromRGB(212, 212, 216),
				surface1 = Color3.fromRGB(218, 218, 223),
				surface0 = Color3.fromRGB(250, 250, 252),
				crust = Color3.fromRGB(175, 175, 180),
				shadow = Color3.fromRGB(150, 150, 160),
			},
			Dark = {
				accent = Color3.fromRGB(10, 132, 255),
				unsafe = Color3.fromRGB(255, 69, 58),
				body = Color3.fromRGB(28, 28, 30),
				text = Color3.fromRGB(255, 255, 255),
				subtext = Color3.fromRGB(99, 99, 102),
				border1 = Color3.fromRGB(56, 56, 58),
				border0 = Color3.fromRGB(44, 44, 46),
				surface1 = Color3.fromRGB(44, 44, 46),
				surface0 = Color3.fromRGB(36, 36, 38),
				crust = Color3.fromRGB(18, 18, 18),
				shadow = Color3.fromRGB(5, 5, 5),
			},
			Catppuccin = {
				accent = Color3.fromRGB(137, 180, 250),
				unsafe = Color3.fromRGB(243, 139, 168),
				body = Color3.fromRGB(30, 30, 46),
				text = Color3.fromRGB(205, 214, 244),
				subtext = Color3.fromRGB(166, 173, 200),
				border1 = Color3.fromRGB(54, 58, 79),
				border0 = Color3.fromRGB(45, 50, 68),
				surface1 = Color3.fromRGB(49, 50, 68),
				surface0 = Color3.fromRGB(36, 39, 58),
				crust = Color3.fromRGB(17, 17, 27),
				shadow = Color3.fromRGB(5, 5, 10),
			},
			Dracula = {
				accent = Color3.fromRGB(189, 147, 249),
				unsafe = Color3.fromRGB(255, 85, 85),
				body = Color3.fromRGB(40, 42, 54),
				text = Color3.fromRGB(248, 248, 242),
				subtext = Color3.fromRGB(98, 114, 164),
				border1 = Color3.fromRGB(68, 71, 90),
				border0 = Color3.fromRGB(58, 60, 78),
				surface1 = Color3.fromRGB(68, 71, 90),
				surface0 = Color3.fromRGB(48, 50, 65),
				crust = Color3.fromRGB(21, 22, 30),
				shadow = Color3.fromRGB(5, 5, 8),
			},
			Nord = {
				accent = Color3.fromRGB(136, 192, 208),
				unsafe = Color3.fromRGB(191, 97, 106),
				body = Color3.fromRGB(46, 52, 64),
				text = Color3.fromRGB(236, 239, 244),
				subtext = Color3.fromRGB(129, 161, 193),
				border1 = Color3.fromRGB(67, 76, 94),
				border0 = Color3.fromRGB(59, 66, 82),
				surface1 = Color3.fromRGB(67, 76, 94),
				surface0 = Color3.fromRGB(59, 66, 82),
				crust = Color3.fromRGB(36, 41, 51),
				shadow = Color3.fromRGB(10, 12, 16),
			},
			Rose = {
				accent = Color3.fromRGB(235, 111, 146),
				unsafe = Color3.fromRGB(235, 111, 146),
				body = Color3.fromRGB(25, 23, 36),
				text = Color3.fromRGB(224, 222, 244),
				subtext = Color3.fromRGB(110, 106, 134),
				border1 = Color3.fromRGB(64, 61, 82),
				border0 = Color3.fromRGB(57, 53, 74),
				surface1 = Color3.fromRGB(64, 61, 82),
				surface0 = Color3.fromRGB(42, 39, 60),
				crust = Color3.fromRGB(14, 12, 23),
				shadow = Color3.fromRGB(5, 4, 10),
			},
			Midnight = {
				accent = Color3.fromRGB(59, 130, 246),
				unsafe = Color3.fromRGB(239, 68, 68),
				body = Color3.fromRGB(17, 17, 19),
				text = Color3.fromRGB(232, 232, 236),
				subtext = Color3.fromRGB(90, 90, 98),
				border1 = Color3.fromRGB(42, 42, 46),
				border0 = Color3.fromRGB(34, 34, 38),
				surface1 = Color3.fromRGB(20, 20, 22),
				surface0 = Color3.fromRGB(26, 26, 29),
				crust = Color3.fromRGB(13, 13, 15),
				shadow = Color3.fromRGB(0, 0, 0),
			},
		},
		_theming = nil,
		_ctx_menu = nil,
		_search_tab_results = {},
		_tab_content_alpha = 1,
		_tab_pill_w_anim = {},
	};
local l = Color3.fromRGB(255, 95, 86);
local O = Color3.fromRGB(196, 68, 62);
local s = Color3.fromRGB(255, 189, 46);
local v = Color3.fromRGB(196, 146, 30);
local W = Color3.fromRGB(39, 201, 63);
local y = Color3.fromRGB(22, 160, 44);
prodordie._theming = prodordie._themes[prodordie._theme_name];
local function Q(T, l)
	return ((type(T) == "number" and T == T)) and T or (l or 0);
end;
local function e(T, l, O)
	return T + ((l - T)) * O;
end;
local function A(T, l, O)
	if T > O then
		return O;
	elseif T < l then
		return l;
	else
		return T;
	end;
end;
local function N(T, l, O)
	return Color3.new(e(T.R, l.R, O), e(T.G, l.G, O), e(T.B, l.B, O));
end;
local function t(T)
	return 1 - ((1 - T)) * ((1 - T));
end;
local function b(T)
	local l = 0;
	for T in pairs(T) do
		l = l + 1;
	end;
	return l;
end;
local function S(T)
	T = Q(T);
	local l = math.floor(T);
	if T == l then
		return tostring(l);
	end;
	return tostring(math.floor(T * 100 + .5) / 100);
end;
local I = {
		lshift = true,
		rshift = true,
		lctrl = true,
		rctrl = true,
		lalt = true,
		ralt = true,
		shift = true,
		ctrl = true,
		alt = true,
	};
local h = {
		f1 = true,
		f2 = true,
		f3 = true,
		f4 = true,
		f5 = true,
		f6 = true,
		f7 = true,
		f8 = true,
		f9 = true,
		f10 = true,
		f11 = true,
		f12 = true,
	};
local c = {
		left = true,
		right = true,
		up = true,
		down = true,
		pageup = true,
		pagedown = true,
		home = true,
		end_ = true,
		insert = true,
		delete = true,
		tab = true,
		enter = true,
		backspace = true,
		esc = true,
		capslock = true,
		pause = true,
		space = true,
	};
local K = { m1 = true, m2 = true, mb = true };
local z = game:GetService("Players");
local B = {};
local u = 0;
local Y = 600;
local function i(T, l, O, s)
	l = l or "";
	local v = tostring(O or T._font_face) .. (":" .. (tostring(s or T._font_size) .. (":" .. l)));
	local W = B[v];
	if W then
		return W;
	end;
	local y = T:_GetTextBounds(l, O, s);
	if u >= Y then
		B = {};
		u = 0;
	end;
	B[v] = y;
	u = u + 1;
	return y;
end;
local function m(T, l)
	return 1 - ((1 - T)) ^ ((l * 60));
end;
local function F(T, l, O)
	local s, v = math.max(T, l, O), math.min(T, l, O);
	local W = s - v;
	local y, Q, e = 0, 0, s;
	if s > 0 then
		Q = W / s;
	end;
	if W > 0 then
		if s == T then
			y = ((l - O)) / W;
			if l < O then
				y = y + 6;
			end;
		elseif s == l then
			y = ((O - T)) / W + 2;
		elseif s == O then
			y = ((T - l)) / W + 4;
		end;
		y = y / 6;
	end;
	return y, Q, e;
end;
prodordie._scroll_delta = 0;
pcall(function()
	local T = z.LocalPlayer;
	if T then
		local l = T:GetMouse();
		if l then
			l.WheelForward:Connect(function()
				prodordie._scroll_delta = prodordie._scroll_delta + 60;
			end);
			l.WheelBackward:Connect(function()
				prodordie._scroll_delta = prodordie._scroll_delta - 60;
			end);
		end;
	end;
end);
do
	local function T()
		return type(writefile) == "function" and type(readfile) == "function";
	end;
	local function l(T)
		if type(makefolder) == "function" then
			pcall(makefolder, T);
		end;
	end;
	local function O(T)
		if type(isfile) == "function" then
			local l, O = pcall(isfile, T);
			return l and O;
		end;
		return false;
	end;
	local function s(T, l)
		if type(writefile) == "function" then
			pcall(writefile, T, l);
		end;
	end;
	local function v(T)
		if type(readfile) == "function" then
			local l, O = pcall(readfile, T);
			if l then
				return O;
			end;
		end;
		return nil;
	end;
	local function W(T)
		if type(listfiles) == "function" then
			local l, O = pcall(listfiles, T);
			if l and type(O) == "table" then
				return O;
			end;
		end;
		return {};
	end;
	local function y(T)
		local l = game:GetService("HttpService");
		local O, s = pcall(function()
				return l:JSONEncode(T);
			end);
		return O and s or nil;
	end;
	local function e(T)
		local l = game:GetService("HttpService");
		local O, s = pcall(function()
				return l:JSONDecode(T);
			end);
		return O and s or nil;
	end;
	function prodordie._RegisterConfigRef(s, T, l, O)
		s._config_refs = s._config_refs or {};
		table.insert(s._config_refs, {
			tab = T,
			sec = l,
			label = O.label,
			item = O,
		});
	end;
	local function A(T)
		if not T then
			return nil;
		end;
		return string.format("%02X%02X%02X", math.floor(T.R * 255 + .5), math.floor(T.G * 255 + .5), math.floor(T.B * 255 + .5));
	end;
	local function N(T)
		if not T or #T < 6 then
			return nil;
		end;
		T = T:match("^#?(%x%x%x%x%x%x)$");
		if not T then
			return nil;
		end;
		return Color3.fromRGB(tonumber(T:sub(1, 2), 16), tonumber(T:sub(3, 4), 16), tonumber(T:sub(5, 6), 16));
	end;
	function prodordie._BuildConfigData(T)
		local l = {};
		for T, O in ipairs(T._config_refs or {}) do
			local s = O.tab .. ("/" .. (O.sec .. ("/" .. O.label)));
			local v = O.item;
			if v.type_ == "toggle" then
				l[s] = v.value;
			elseif v.type_ == "slider" then
				l[s] = v.value;
			elseif v.type_ == "dropdown" then
				l[s] = type(v.value) == "table" and table.concat(v.value, ",") or tostring(v.value or "");
			elseif v.type_ == "textbox" then
				l[s] = v.value or "";
			end;
			if v.keybind then
				l[s .. "__kb"] = v.keybind.value or "";
			end;
			if v.colorpicker then
				l[s .. "__cp"] = A(v.colorpicker.value);
			end;
		end;
		l.__theme = T._theme_name;
		l.__menukey = T._menu_key;
		return l;
	end;
	function prodordie._ApplyConfigData(l, T)
		if type(T) ~= "table" then
			return;
		end;
		for O, s in ipairs(l._config_refs or {}) do
			local v = s.tab .. ("/" .. (s.sec .. ("/" .. s.label)));
			local W = s.item;
			local y = T[v];
			local Q = W._rawcb;
			if y ~= nil then
				if W.type_ == "toggle" then
					W.value = (y == true or y == "true");
					if Q then
						pcall(Q, W.value);
					end;
					local T = s.tab .. (s.sec .. s.label);
					l._toggle_anim[T] = W.value and 1 or 0;
				elseif W.type_ == "slider" then
					local T = tonumber(y);
					if T then
						W.value = T;
						if Q then
							pcall(Q, T);
						end;
					end;
				elseif W.type_ == "dropdown" then
					local T = {};
					for l in (tostring(y)):gmatch("[^,]+") do
						table.insert(T, l);
					end;
					W.value = T;
					if Q then
						pcall(Q, W.value);
					end;
				elseif W.type_ == "textbox" then
					W.value = tostring(y);
					if Q then
						pcall(Q, W.value);
					end;
				end;
			end;
			if W.keybind then
				local l = T[v .. "__kb"];
				if l ~= nil then
					W.keybind.value = tostring(l);
				end;
			end;
			if W.colorpicker then
				local l = T[v .. "__cp"];
				if l then
					local T = N(tostring(l));
					if T then
						W.colorpicker.value = T;
						if W.colorpicker.callback then
							pcall(W.colorpicker.callback, T);
						end;
					end;
				end;
			end;
		end;
		if T.__theme and l._themes[T.__theme] then
			l:_ApplyTheme(T.__theme);
		end;
		if T.__menukey and l._inputs[T.__menukey] then
			l._menu_key = T.__menukey;
		end;
		for l, O in ipairs(l._config_callbacks or {}) do
			pcall(O, T);
		end;
	end;
	function prodordie._AutoSaveTick(v, O)
		if not v._autosave_dirty then
			return;
		end;
		v._autosave_timer = v._autosave_timer + Q(O, .016);
		if v._autosave_timer < v._autosave_delay then
			return;
		end;
		v._autosave_dirty = false;
		v._autosave_timer = 0;
		v._autosave_last = os.clock();
		if not T() then
			return;
		end;
		l(v._config_folder);
		local W = v:_BuildConfigData();
		if v._config_name and v._config_name ~= "default" then
			W.__savedas = v._config_name;
		end;
		local e = y(W);
		if not e then
			return;
		end;
		s(v._config_folder .. "/_last.json", e);
	end;
	function prodordie._RefreshConfigList(T)
		local l = T:ListConfigs();
		local O = {};
		local s = false;
		for T, l in ipairs(l or {}) do
			if l ~= "_last" then
				table.insert(O, l);
				if l == "default" then
					s = true;
				end;
			end;
		end;
		if not s then
			table.insert(O, 1, "default");
		end;
		T._config_list = O;
		return O;
	end;
	function prodordie.SaveConfig(v, O)
		O = tostring(O or v._config_name or "default");
		if O == "default" or O == "" then
			v:Notification("Set a config name first", 5);
			return;
		end;
		if not T() then
			v:Notification("No filesystem API", 5);
			return;
		end;
		l(v._config_folder);
		local W = v:_BuildConfigData();
		W.__savedas = O;
		local Q = y(W);
		if not Q then
			v:Notification("Encode failed", 4);
			return;
		end;
		s(v._config_folder .. ("/" .. (O .. ".json")), Q);
		s(v._config_folder .. "/_last.json", Q);
		v._autosave_dirty = false;
		v._autosave_timer = 0;
		v._config_name = O;
		v:_RefreshConfigList();
		v:Notification("Saved: " .. O, 4);
	end;
	function prodordie.LoadConfig(s, l)
		l = tostring(l or s._config_name or "default");
		if not T() then
			s:Notification("No filesystem API", 5);
			return;
		end;
		local W = s._config_folder .. ("/" .. (l .. ".json"));
		if not O(W) then
			if l ~= "_last" then
				s:Notification("Not found: " .. l, 4);
			end;
			return;
		end;
		local y = v(W);
		if not y then
			s:Notification("Read failed: " .. l, 4);
			return;
		end;
		local Q = e(y);
		if not Q then
			s:Notification("Config corrupted, using defaults", 4);
			if type(delfile) == "function" then
				pcall(delfile, W);
			end;
			return;
		end;
		s:_ApplyConfigData(Q);
		if l ~= "_last" then
			s._config_name = l;
			s:_RefreshConfigList();
			s:Notification("Loaded: " .. l, 4);
		end;
	end;
	function prodordie.DeleteConfig(O, l)
		l = tostring(l or O._config_name or "default");
		if l == "default" then
			O:Notification("Cannot delete default config", 4);
			return;
		end;
		if not T() then
			return;
		end;
		local s = O._config_folder .. ("/" .. (l .. ".json"));
		if type(delfile) == "function" then
			pcall(delfile, s);
		end;
		if O._config_name == l then
			O._config_name = "default";
		end;
		O:_RefreshConfigList();
		O:Notification("Deleted: " .. l, 4);
	end;
	function prodordie.ListConfigs(O)
		if not T() then
			return {};
		end;
		l(O._config_folder);
		local s = W(O._config_folder);
		local v = {};
		for T, l in ipairs(s or {}) do
			local O = (tostring(l)):gsub("\\", "/");
			local s = O:match("([^/]+)%.json$");
			if s then
				table.insert(v, s);
			end;
		end;
		return v;
	end;
	function prodordie._MarkDirty(T)
		if not T._autosave_dirty then
			T._autosave_timer = 0;
		end;
		T._autosave_dirty = true;
	end;
	function prodordie.AutoLoadConfig(T)
		T._autoload_requested = true;
	end;
	function prodordie._DoAutoLoad(W)
		if not T() then
			return;
		end;
		local Q = W._config_folder;
		local A = Q .. "/_last.json";
		local N = Q .. "/default.json";
		l(Q);
		if O(A) then
			local T = v(A);
			local l = T and e(T);
			if l then
				W:_ApplyConfigData(l);
				local T = ((l.__savedas and l.__savedas ~= "")) and ("\"" .. (tostring(l.__savedas) .. "\"")) or "last session";
				W:Notification("Config restored: " .. T, 4);
				W._config_name = l.__savedas or "default";
				return;
			else
				W:Notification("Config corrupted, using defaults", 4);
				if type(delfile) == "function" then
					pcall(delfile, A);
				end;
			end;
		end;
		if O(N) then
			local T = v(N);
			local l = T and e(T);
			if l then
				W:_ApplyConfigData(l);
			end;
			return;
		end;
		local t = W:_BuildConfigData();
		local b = y(t);
		if b then
			s(N, b);
		end;
	end;
end;
do
	function prodordie._GetSec(l, T)
		l._sec_scroll = l._sec_scroll or {};
		if not l._sec_scroll[T] then
			l._sec_scroll[T] = { offset = 0, target = 0, maxScroll = 0 };
		end;
		return l._sec_scroll[T];
	end;
	function prodordie._TickScroll(O, T, l)
		local s = O:_GetSec(T);
		s.offset = e(s.offset, s.target, m(.12, l));
		if math.abs(s.offset - s.target) < .5 then
			s.offset = s.target;
		end;
		return s.offset;
	end;
	function prodordie._ScrollSection(O, T, l)
		local s = O:_GetSec(T);
		s.target = A(s.target + l, 0, s.maxScroll);
	end;
end;
do
	function prodordie._KeyIDToName(l, T)
		for l, O in pairs(l._inputs or {}) do
			if O.id == T then
				return l;
			end;
		end;
		return nil;
	end;
	function prodordie._IsKeyPressed(l, T)
		return l._inputs and (l._inputs[T] and l._inputs[T].click);
	end;
	function prodordie._IsKeyHeld(l, T)
		return l._inputs and (l._inputs[T] and l._inputs[T].held);
	end;
	function prodordie._GetScreenSize(T)
		local l = os.clock();
		if not T._cached_ss or l - ((T._cached_ss_at or 0)) > 1 then
			local O = Vector2.new(1920, 1080);
			local s = workspace.CurrentCamera;
			if s and s.ViewportSize then
				O = s.ViewportSize;
			end;
			T._cached_ss = O;
			T._cached_ss_at = l;
		end;
		return T._cached_ss;
	end;
	function prodordie._GetMousePos(T)
		return T._cached_mouse or Vector2.new();
	end;
	function prodordie._PollMousePos(T)
		local l = z.LocalPlayer;
		if l then
			local O = l:GetMouse();
			if O then
				local l, s = O.X, O.Y;
				local v = T._cached_mouse;
				if not v or v.x ~= l or v.y ~= s then
					T._cached_mouse = Vector2.new(l, s);
				end;
				return;
			end;
		end;
		if not T._cached_mouse then
			T._cached_mouse = Vector2.new();
		end;
	end;
	function prodordie._IsMouseWithinBounds(O, T, l)
		local s = O:_GetMousePos();
		return s.x >= T.x and (s.x <= T.x + l.x and (s.y >= T.y and s.y <= T.y + l.y));
	end;
	function prodordie._ApplyTheme(l, T)
		l._theme_name = T;
		l._theming = l._themes[T] or l._themes.Light;
	end;
end;
do
	function prodordie._GetTextBounds(s, T, l, O)
		if not T or T == "" then
			return Vector2.new(0, O or s._font_size);
		end;
		l = l or s._font_face;
		O = O or s._font_size;
		if l == Drawing.Fonts.UI then
			return Vector2.new((#T * O) * .53846, O);
		end;
		return Vector2.new((#T * O) * .58, O);
	end;
	function prodordie._Draw(v, T, l, O, s, ...)
		local W = T == "rect" and "Square" or (T == "text" and "Text" or (T == "circle" and "Circle" or (T == "triangle" and "Triangle" or "Line")));
		if T == "gradient" then
			local T, l, W = ...;
			local y = { select(4, ...) };
			local Q = #y - 1;
			local A = 10;
			for N = 1, Q, 1 do
				local t, b = y[N], y[N + 1];
				local S, I = math.max(1, W.x) / Q, math.max(1, W.y) / Q;
				for y = 1, A, 1 do
					local Q = ((y - 1)) / ((A - 1));
					local h = Color3.new(e(t.R, b.R, Q), e(t.G, b.G, Q), e(t.B, b.B, Q));
					local c = e(t.A or 1, b.A or 1, Q) * s;
					local K, z;
					if T == "horizontal" then
						z = Vector2.new(S / A, W.y);
						K = Vector2.new((l.x + ((N - 1)) * S) + ((y - 1)) * z.x, l.y);
					elseif T == "vertical" then
						z = Vector2.new(W.x, I / A);
						K = Vector2.new(l.x, (l.y + ((N - 1)) * I) + ((y - 1)) * z.y);
					end;
					v:_Draw("rect", h, O, c, K, z, true);
				end;
			end;
			return;
		end;
		v._pIdx[W] = v._pIdx[W] + 1;
		local y = v._pIdx[W];
		local Q = v._pool[W][y];
		local N = v._pool_state[W][y];
		if not Q then
			Q = Drawing.new(W);
			Q.Visible = false;
			v._pool[W][y] = Q;
			N = { Visible = false };
			v._pool_state[W][y] = N;
		end;
		if N.ZIndex ~= O then
			Q.ZIndex = O;
			N.ZIndex = O;
		end;
		if l ~= nil and N.Color ~= l then
			Q.Color = l;
			N.Color = l;
		end;
		local t = A(s, 0, 1);
		if N.Transparency ~= t then
			Q.Transparency = t;
			N.Transparency = t;
		end;
		if N.Visible ~= true then
			Q.Visible = true;
			N.Visible = true;
		end;
		if T == "rect" then
			local T, l, O, s = ...;
			if N.Position ~= T then
				Q.Position = T;
				N.Position = T;
			end;
			if N.Size ~= l then
				Q.Size = l;
				N.Size = l;
			end;
			if N.Filled ~= O then
				Q.Filled = O;
				N.Filled = O;
			end;
			if s and s > 0 then
				if N.Corner ~= s then
					Q.Corner = s;
					N.Corner = s;
				end;
			else
				if N.Corner ~= 0 then
					Q.Corner = 0;
					N.Corner = 0;
				end;
			end;
		elseif T == "text" then
			local T, l, O, s, W, y = ...;
			local e = (s == "center");
			if N.Center ~= e then
				Q.Center = e;
				N.Center = e;
			end;
			if N.Position ~= T then
				Q.Position = T;
				N.Position = T;
			end;
			if N.Text ~= l then
				Q.Text = l;
				N.Text = l;
			end;
			if N.Outline ~= O then
				Q.Outline = O;
				N.Outline = O;
			end;
			local A = y or v._font_face;
			if N.Font ~= A then
				Q.Font = A;
				N.Font = A;
			end;
			local t = W or v._font_size;
			if N.Size ~= t then
				Q.Size = t;
				N.Size = t;
			end;
		elseif T == "triangle" then
			local T, l, O, s = ...;
			if N.Filled ~= T then
				Q.Filled = T;
				N.Filled = T;
			end;
			if N.PointA ~= l then
				Q.PointA = l;
				N.PointA = l;
			end;
			if N.PointB ~= O then
				Q.PointB = O;
				N.PointB = O;
			end;
			if N.PointC ~= s then
				Q.PointC = s;
				N.PointC = s;
			end;
		elseif T == "circle" then
			local T, l, O = ...;
			if N.Position ~= T then
				Q.Position = T;
				N.Position = T;
			end;
			if N.Radius ~= l then
				Q.Radius = l;
				N.Radius = l;
			end;
			if N.Filled ~= O then
				Q.Filled = O;
				N.Filled = O;
			end;
		elseif T == "line" then
			local T, l, O = ...;
			if N.From ~= T then
				Q.From = T;
				N.From = T;
			end;
			if N.To ~= l then
				Q.To = l;
				N.To = l;
			end;
			local s = O or 1;
			if N.Thickness ~= s then
				Q.Thickness = s;
				N.Thickness = s;
			end;
		end;
	end;
	function prodordie._FullWipe(T)
		for T, l in pairs(T._pool or {}) do
			for T, l in ipairs(l) do
				l.Visible = false;
				pcall(function()
					l:Remove();
				end);
			end;
		end;
		T._pool = {
				Square = {},
				Text = {},
				Triangle = {},
				Circle = {},
				Line = {},
			};
		T._pool_state = {
				Square = {},
				Text = {},
				Triangle = {},
				Circle = {},
				Line = {},
			};
	end;
end;
do
	function prodordie._SpawnColorpicker(v, T, l, O, s)
		local W, y, Q = F(O.R, O.G, O.B);
		v._active_colorpicker = {
				position = T or Vector2.new((v.x + v.w) + v._padding, v.y),
				label = l,
				callback = s,
				_h = W,
				_s = y,
				_v = Q,
				_anim = 0,
				_closing = false,
			};
	end;
	function prodordie._RemoveColorpicker(T)
		if T._active_colorpicker then
			T._active_colorpicker._closing = true;
		end;
	end;
	function prodordie._SpawnDropdown(y, T, l, O, s, v, W)
		y._active_dropdown = {
				position = T,
				width = l,
				value = O,
				choices = s,
				multi = v,
				callback = W,
				_spawned_at = os.clock(),
				_anim = 0,
			};
		y._dropdown_scroll_target = 0;
		y._dropdown_scroll_v = 0;
	end;
	function prodordie._RemoveDropdown(T)
		T._active_dropdown = nil;
	end;
	function prodordie._SpawnCtxMenu(v, T, l, O, s)
		v._ctx_menu = {
				x = T,
				y = l,
				siid = O,
				sItem = s,
				_anim = 0,
				items = { { label = "Copy", action = "copy" }, { label = "Clear", action = "clear" } },
			};
	end;
	function prodordie._RemoveCtxMenu(T)
		T._ctx_menu = nil;
	end;
end;
do
	function prodordie._Toggle(Q, T, l, O, s, v, W, y)
		if type(O) == "table" then
			local T = O;
			O = T.Label or T.label;
			s = T.Value or T.value;
			v = T.Callback or T.callback;
			W = T.IsNew or T.isNew;
			y = T.Tooltip or T.tooltip;
		end;
		Q._tree = Q._tree or {};
		Q._tree[T] = Q._tree[T] or { _items = {}, _searchQuery = "" };
		Q._tree[T]._items[l] = Q._tree[T]._items[l] or { _items = {}, _count = 0 };
		local e = #Q._tree[T]._items[l]._items + 1;
		local function A(T)
			Q:_MarkDirty();
			if v then
				v(T);
			end;
		end;
		local N = T .. ("_" .. (l .. ("_" .. e)));
		local t = {
				type_ = "toggle",
				_rawcb = v,
				label = O,
				value = s,
				callback = A,
				isNew = W or false,
				tooltip = y,
				_id = N,
				_anim_key = "s_anim_" .. N,
			};
		table.insert(Q._tree[T]._items[l]._items, t);
		local b = T .. (l .. tostring(e));
		Q._toggle_anim[b] = s and 1 or 0;
		Q:_RegisterConfigRef(T, l, t);
		return { Set = function(O, s)
				Q._tree[T]._items[l]._items[e].value = s;
				if Q._tree[T]._items[l]._items[e].callback then
					Q._tree[T]._items[l]._items[e].callback(s);
				end;
			end, AddKeybind = function(O, s, v)
				local W = {
						value = s,
						callback = v,
						_listening = false,
						_listening_start = 0,
					};
				Q._tree[T]._items[l]._items[e].keybind = W;
				return { Set = function(O, s)
						Q._tree[T]._items[l]._items[e].keybind.value = s;
						if Q._tree[T]._items[l]._items[e].keybind.callback then
							Q._tree[T]._items[l]._items[e].keybind.callback(s);
						end;
					end };
			end, AddColorpicker = function(O, s, v, W, y)
				local A = {
						label = s,
						value = v or Q._theming.accent,
						overwrite = W,
						callback = y,
					};
				Q._tree[T]._items[l]._items[e].colorpicker = A;
				return { Set = function(O, s)
						Q._tree[T]._items[l]._items[e].colorpicker.value = s;
						if Q._tree[T]._items[l]._items[e].colorpicker.callback then
							Q._tree[T]._items[l]._items[e].colorpicker.callback(s);
						end;
					end };
			end };
	end;
	function prodordie._Slider(N, T, l, O, s, v, W, y, Q, e, A)
		if type(O) == "table" then
			local T = O;
			O = T.Label or T.label;
			s = T.Value or T.value;
			v = T.Step or T.step;
			W = T.Min or T.min;
			y = T.Max or T.max;
			Q = T.Suffix or T.suffix;
			e = T.IsNew or T.isNew;
			A = T.Callback or T.callback;
		elseif type(e) == "function" then
			A = e;
			e = false;
		end;
		N._tree = N._tree or {};
		N._tree[T] = N._tree[T] or { _items = {}, _searchQuery = "" };
		N._tree[T]._items[l] = N._tree[T]._items[l] or { _items = {}, _count = 0 };
		local t = #N._tree[T]._items[l]._items + 1;
		local function b(T)
			N:_MarkDirty();
			if A then
				A(T);
			end;
		end;
		local S = T .. ("_" .. (l .. ("_" .. t)));
		local I = {
				type_ = "slider",
				_rawcb = A,
				label = O,
				value = s,
				default = s,
				step = v,
				min = W,
				max = y,
				suffix = Q or "",
				isNew = e or false,
				callback = b,
				_id = S,
				_anim_key = "s_anim_" .. S,
			};
		table.insert(N._tree[T]._items[l]._items, I);
		N:_RegisterConfigRef(T, l, I);
		return { Set = function(O, s)
				N._tree[T]._items[l]._items[t].value = s;
				if N._tree[T]._items[l]._items[t].callback then
					N._tree[T]._items[l]._items[t].callback(s);
				end;
			end };
	end;
	function prodordie._Dropdown(e, T, l, O, s, v, W, y, Q)
		if type(O) == "table" then
			local T = O;
			O = T.Label or T.label;
			s = T.Value or T.value;
			v = T.Choices or T.choices;
			W = T.Multi or T.multi;
			y = T.IsNew or T.isNew;
			Q = T.Callback or T.callback;
		elseif type(y) == "function" then
			Q = y;
			y = false;
		end;
		e._tree = e._tree or {};
		e._tree[T] = e._tree[T] or { _items = {}, _searchQuery = "" };
		e._tree[T]._items[l] = e._tree[T]._items[l] or { _items = {}, _count = 0 };
		local A = #e._tree[T]._items[l]._items + 1;
		local function N(T)
			e:_MarkDirty();
			if Q then
				Q(T);
			end;
		end;
		local t = T .. ("_" .. (l .. ("_" .. A)));
		local b = {
				type_ = "dropdown",
				_rawcb = Q,
				label = O,
				value = s,
				choices = v,
				multi = W,
				isNew = y or false,
				callback = N,
				_id = t,
				_anim_key = "s_anim_" .. t,
			};
		table.insert(e._tree[T]._items[l]._items, b);
		e:_RegisterConfigRef(T, l, b);
		return { Set = function(O, s)
				e._tree[T]._items[l]._items[A].value = s;
				if e._tree[T]._items[l]._items[A].callback then
					e._tree[T]._items[l]._items[A].callback(s);
				end;
			end, UpdateChoices = function(O, s)
				e._tree[T]._items[l]._items[A].choices = s;
			end };
	end;
	function prodordie._Button(W, T, l, O, s, v)
		if type(O) == "table" then
			local T = O;
			O = T.Label or T.label;
			s = T.IsNew or T.isNew;
			v = T.Callback or T.callback;
		elseif type(s) == "function" then
			v = s;
			s = false;
		end;
		W._tree = W._tree or {};
		W._tree[T] = W._tree[T] or { _items = {}, _searchQuery = "" };
		W._tree[T]._items[l] = W._tree[T]._items[l] or { _items = {}, _count = 0 };
		local y = #W._tree[T]._items[l]._items + 1;
		local Q = T .. ("_" .. (l .. ("_" .. y)));
		local e = {
				type_ = "button",
				label = O,
				isNew = s or false,
				callback = v,
				_id = Q,
				_anim_key = "s_anim_" .. Q,
			};
		table.insert(W._tree[T]._items[l]._items, e);
		return { AddColorpicker = function(O, s, v, Q, e)
				local A = {
						label = s,
						value = v or W._theming.accent,
						overwrite = Q,
						callback = e,
					};
				W._tree[T]._items[l]._items[y].colorpicker = A;
				return { Set = function(O, s)
						W._tree[T]._items[l]._items[y].colorpicker.value = s;
						if W._tree[T]._items[l]._items[y].colorpicker.callback then
							W._tree[T]._items[l]._items[y].colorpicker.callback(s);
						end;
					end };
			end };
	end;
	function prodordie._Textbox(W, T, l, O, s, v)
		if type(O) == "table" then
			local T = O;
			O = T.Label or T.label;
			s = T.Value or T.value;
			v = T.Callback or T.callback;
		end;
		W._tree = W._tree or {};
		W._tree[T] = W._tree[T] or { _items = {}, _searchQuery = "" };
		W._tree[T]._items[l] = W._tree[T]._items[l] or { _items = {}, _count = 0 };
		local y = #W._tree[T]._items[l]._items + 1;
		local function Q(T)
			W:_MarkDirty();
			if v then
				v(T);
			end;
		end;
		local e = T .. ("_" .. (l .. ("_" .. y)));
		local A = {
				type_ = "textbox",
				_rawcb = v,
				label = O,
				value = s,
				callback = Q,
				_id = e,
				_anim_key = "s_anim_" .. e,
			};
		table.insert(W._tree[T]._items[l]._items, A);
		W:_RegisterConfigRef(T, l, A);
		return { Set = function(O, s)
				W._tree[T]._items[l]._items[y].value = s;
				if W._tree[T]._items[l]._items[y].callback then
					W._tree[T]._items[l]._items[y].callback(s);
				end;
			end };
	end;
	function prodordie._Section(O, T, l)
		O._tree = O._tree or {};
		O._tree[T] = O._tree[T] or { _items = {}, _searchQuery = "" };
		O._tree[T]._items[l] = { _items = {}, _count = 0 };
		O._tree[T]._secCount = ((O._tree[T]._secCount or 0)) + 1;
		if not O._tree[T]._sec_order then
			O._tree[T]._sec_order = {};
		end;
		table.insert(O._tree[T]._sec_order, l);
		return {
			Toggle = function(s, ...)
				return O:_Toggle(T, l, ...);
			end,
			Slider = function(s, ...)
				return O:_Slider(T, l, ...);
			end,
			Dropdown = function(s, ...)
				return O:_Dropdown(T, l, ...);
			end,
			Button = function(s, ...)
				return O:_Button(T, l, ...);
			end,
			Textbox = function(s, ...)
				return O:_Textbox(T, l, ...);
			end,
		};
	end;
end;
do
	function prodordie.GetMenuSize(T)
		return Vector2.new(T.w, T.h);
	end;
	function prodordie.SetWatermarkEnabled(l, T)
		l._watermark_enabled = T;
	end;
	function prodordie.SetMenuTitle(l, T)
		local O = tostring(T);
		if #O > 32 then
			O = O:sub(1, 32);
		end;
		l.title = O;
		l._custom_title = O;
	end;
	function prodordie.SetMenuPosition(l, T)
		l.x = T.x or l.x;
		l.y = T.y or l.y;
	end;
	function prodordie.SetMenuSize(l, T)
		l.w = T.x or l.w;
		l.h = T.y or l.h;
	end;
	function prodordie.SetMenuKey(l, T)
		local O = (tostring(T)):lower();
		if l._inputs[O] then
			l._menu_key = O;
			l:Notification("Menu key set to " .. O:upper(), 3);
		else
			l:Notification("Invalid key: " .. O, 3);
		end;
	end;
	function prodordie.CenterMenu(T)
		local l = T:_GetScreenSize();
		local O = T:GetMenuSize();
		T:SetMenuPosition(Vector2.new(l.x / 2 - O.x / 2, l.y / 2 - O.y / 2));
	end;
	function prodordie.Notification(O, T, l)
		local s = tostring(T);
		O._notifications = O._notifications or {};
		for T, O in ipairs(O._notifications) do
			if O.text == s then
				if os.clock() - O._spawned_at < 1 then
					return;
				end;
				O._spawned_at = os.clock();
				O.time = l;
				return;
			end;
		end;
		if #O._notifications >= 6 then
			table.remove(O._notifications, 1);
		end;
		table.insert(O._notifications, {
			text = s,
			time = l or 3,
			_id = O._notifications_spawned,
			_spawned_at = os.clock(),
		});
		O._notifications_spawned = O._notifications_spawned + 1;
	end;
	function prodordie.Tab(l, T)
		l._tree = l._tree or {};
		l._tree[T] = { _items = {}, _searchQuery = "" };
		l._tab_order = l._tab_order or {};
		table.insert(l._tab_order, T);
		if not l._open_tab then
			l._open_tab = T;
			l._tab_fade = 1;
		end;
		if not l._autoload_requested and not l._boot_loaded then
			l:AutoLoadConfig();
		end;
		return { Section = function(O, s)
				return l:_Section(T, s);
			end };
	end;
	function prodordie.RegisterActivity(l, T)
		l._activities = l._activities or {};
		local O = #l._activities + 1;
		l._activities[O] = T;
		return { Remove = function(T)
				l._activities[O] = nil;
			end };
	end;
	function prodordie.Unload(T)
		T._unloading_sequence = true;
		T._unload_phase = "sections";
		T._unload_section_alpha = 1;
	end;
	function prodordie.CreateSettingsTab(l, T)
		local O = l:Tab(T or "Settings");
		local s = T or "Settings";
		l._tab_order = l._tab_order or {};
		for T, O in ipairs(l._tab_order) do
			if O == s then
				table.remove(l._tab_order, T);
				break;
			end;
		end;
		table.insert(l._tab_order, s);
		local v = O:Section("Menu");
		local W = v:Toggle("Menu Key", l._overwrite_menu_key, function(T)
				l._overwrite_menu_key = T;
			end);
		W:AddKeybind(l._menu_key, function(T)
			l._menu_key = l:_KeyIDToName(T);
		end);
		v:Toggle("Watermark", true, function(T)
			l:SetWatermarkEnabled(T);
		end);
		v:Toggle("Custom watermark", l._custom_title_enabled, function(T)
			l._custom_title_enabled = T;
		end);
		v:Textbox("Watermark text", l.title, function(T)
			local O = tostring(T);
			if #O > 32 then
				O = O:sub(1, 32);
			end;
			l._custom_title = O;
		end);
		v:Button("Unload Script", false, function()
			l:Unload();
		end);
		local y = O:Section("Config & Theme");
		local Q = {};
		for T in pairs(l._themes or {}) do
			table.insert(Q, T);
		end;
		table.sort(Q);
		y:Dropdown("Theme", { l._theme_name }, Q, false, false, function(T)
			if T and T[1] then
				l:_ApplyTheme(T[1]);
			end;
		end);
		y:Textbox("New config name", "", function(T)
			if T and T ~= "" then
				l._config_name = T;
			end;
		end);
		l:_RefreshConfigList();
		local e = l._config_list;
		if #e == 0 then
			e = { "default" };
		end;
		local A = y:Dropdown("Saved configs", { l._config_name }, e, false, false, function(T)
				if T and T[1] then
					l._config_name = T[1];
				end;
			end);
		y:Button("Save config", false, function()
			l:SaveConfig(l._config_name);
			local T = l:_RefreshConfigList();
			if #T == 0 then
				T = { "default" };
			end;
			A:UpdateChoices(T);
		end);
		y:Button("Load config", false, function()
			l:LoadConfig(l._config_name);
		end);
		y:Button("Delete config", false, function()
			l:DeleteConfig(l._config_name);
			local T = l:_RefreshConfigList();
			if #T == 0 then
				T = { "default" };
			end;
			A:UpdateChoices(T);
			l._config_name = T[1] or "default";
		end);
		return O, v, y;
	end;
end;
local function o(T, l)
	local O = l.type_;
	if O == "toggle" then
		return (T._font_size + 2) + T._padding;
	elseif O == "slider" then
		local O = i(T, l.label);
		return ((O.y + 6) + 18) + T._padding * 3;
	elseif O == "dropdown" then
		local O = i(T, l.label);
		return O.y * 2 + T._padding * 4;
	elseif O == "button" then
		local O = i(T, l.label);
		return O.y + T._padding * 3;
	elseif O == "textbox" then
		return T._font_size + T._padding * 2;
	end;
	return 0;
end;
local function M(T, l)
	local O = T._search_lower;
	if not O or O == "" then
		return true;
	end;
	if not l._lower_label then
		l._lower_label = l.label:lower();
	end;
	return l._lower_label:find(O, 1, true) ~= nil;
end;
function prodordie.Step(T)
	if T._unloaded then
		return;
	end;
	T._pIdx = {
			Square = 0,
			Text = 0,
			Triangle = 0,
			Circle = 0,
			Line = 0,
		};
	T._tree = T._tree or {};
	T._tab_order = T._tab_order or {};
	T._sec_scroll = T._sec_scroll or {};
	T._sec_slide = T._sec_slide or {};
	T._inputs = T._inputs or {};
	T._item_vis = T._item_vis or {};
	T._tab_scroll = T._tab_scroll or {};
	T._frame_tooltip = nil;
	local Q = T._theming;
	local b = T._corner;
	local z = T._padding;
	local B = T._KB_W;
	local u = os.clock();
	local Y = math.min(u - T._last_step_time, .05);
	if T._last_step_time == 0 then
		Y = .016;
	end;
	T._last_step_time = u;
	local function F(T)
		return m(T, Y);
	end;
	if T._tab_changing then
		T._tab_fade = e(T._tab_fade, 0, F(.45));
		if T._tab_fade < .02 then
			T._open_tab = T._next_tab;
			T._tab_changing = false;
			T._tab_fade = 0;
			T._sec_slide = {};
			T._item_vis = {};
		end;
	else
		T._tab_fade = e(T._tab_fade, 1, F(.18));
	end;
	local f = t(T._tab_fade);
	T:_PollMousePos();
	if T._autoload_requested and not T._boot_loaded then
		T._boot_loaded = true;
		T._autoload_requested = false;
		task.spawn(function()
			T:_DoAutoLoad();
		end);
	end;
	T:_AutoSaveTick(Y);
	local w = game:GetService("UserInputService");
	local V = false;
	pcall(function()
		V = w:GetFocusedTextBox() ~= nil;
	end);
	local J = true;
	if type(isrbxactive) == "function" then
		pcall(function()
			J = isrbxactive();
		end);
	end;
	if not T._kcCache then
		T._kcCache = {};
		for l, O in pairs(T._inputs) do
			if l == "m1" then
				T._kcCache[l] = Enum.UserInputType.MouseButton1;
			elseif l == "m2" then
				T._kcCache[l] = Enum.UserInputType.MouseButton2;
			elseif l == "mb" then
				T._kcCache[l] = Enum.UserInputType.MouseButton3;
			else
				pcall(function()
					T._kcCache[l] = Enum.KeyCode[l:upper()];
				end);
			end;
		end;
	end;
	for l, O in pairs(T._inputs) do
		local s = false;
		if type(iskeypressed) == "function" then
			pcall(function()
				s = iskeypressed(O.id);
			end);
		else
			local O = T._kcCache[l];
			if O then
				if l == "m1" or l == "m2" or l == "mb" then
					s = w:IsMouseButtonPressed(O) == true;
				else
					s = w:IsKeyDown(O) == true;
				end;
			end;
		end;
		if not V and (J and s) then
			T._inputs[l].click = (not O.held and not O.click);
			T._inputs[l].held = true;
		else
			T._inputs[l].click = false;
			T._inputs[l].held = false;
		end;
	end;
	local X = T:_IsKeyPressed("m1");
	local P = T:_IsKeyHeld("m1");
	local L = T:_IsKeyPressed("m2");
	local r = (tostring(T._overwrite_menu_key and T._menu_key or "f5")):lower();
	local n = T:_IsKeyPressed(r);
	local k = false;
	local j = false;
	for T, l in pairs(T._inputs) do
		if not K[T] and (not I[T] and (not h[T] and (not c[T] and l.click))) then
			j = true;
			break;
		end;
	end;
	local Z = X and not j;
	if n then
		T._menu_open = not T._menu_open;
		if not T._menu_open then
			T:_RemoveDropdown();
			T:_RemoveColorpicker();
			T:_RemoveCtxMenu();
			T:Notification("Menu Minimized (" .. (r:upper() .. " to open)"), 3);
		end;
	end;
	if T._unloading_sequence then
		if T._unload_phase == "sections" then
			T._unload_section_alpha = e(T._unload_section_alpha or 1, 0, F(.18));
			if ((T._unload_section_alpha or 0)) < .04 then
				T._unload_section_alpha = 0;
				T._unload_phase = "menu";
				T._menu_open = false;
			end;
		end;
	end;
	local d = ((T._menu_open and not ((T._unload_phase == "menu" or T._unload_phase == "done")))) and 1 or 0;
	local a = T._menu_scale;
	if a ~= d then
		a = e(a, d, F(.12));
		if math.abs(a - d) < .002 then
			a = d;
		end;
		T._menu_scale = a;
	end;
	if T._unloading_sequence and (T._unload_phase == "menu" and a < .01) then
		T._unloaded = true;
		T:_FullWipe();
		return;
	end;
	local H = T.x;
	local E = T.y;
	local D = T.w;
	local p = T._custom_title_enabled and T._custom_title or T.title;
	local g = ((T:_GetTextBounds(p)).y + z * 2) + 4;
	local G = T._search_query;
	if G ~= T._search_query_last then
		T._search_query_last = G;
		T._search_lower = G ~= "" and G:lower() or "";
		local l = {};
		if G ~= "" then
			local O = T._search_lower;
			for s, v in ipairs(T._tab_order) do
				local W = T._tree[v];
				local y = 0;
				if W then
					for T, l in ipairs(W._sec_order or {}) do
						local s = W._items[l] or { _items = {} };
						for T, l in ipairs(s._items or {}) do
							if not l._lower_label then
								l._lower_label = l.label:lower();
							end;
							if l._lower_label:find(O, 1, true) then
								y = y + 1;
							end;
						end;
					end;
				end;
				l[v] = y;
			end;
		end;
		T._search_tab_results = l;
	end;
	if not T._pill_x then
		T._pill_x = 0;
	end;
	if not T._pill_target_x then
		T._pill_target_x = 0;
	end;
	local R = T.h;
	local C = T._tree[T._open_tab];
	local q, U = {}, {};
	local x, TN = z, z;
	if C then
		for l, O in ipairs(C._sec_order or {}) do
			local s = C._items[O] or { _items = {} };
			local v = i(T, O);
			local W = z + v.y / 2;
			local y = 0;
			for l, O in ipairs(s._items or {}) do
				local s = M(T, O);
				if s then
					y = y + 1;
				end;
				local v = O._anim_key;
				local Q = s and 1 or 0;
				local A = T._toggle_anim[v];
				if A == nil then
					A = Q;
				end;
				if A ~= Q then
					A = e(A, Q, F(.15));
				end;
				T._toggle_anim[v] = A;
				if A > .001 then
					W = W + o(T, O) * A;
				end;
			end;
			local Q = "sec_anim_" .. (T._open_tab .. ("_" .. O));
			local A = y > 0 and 1 or 0;
			T._toggle_anim[Q] = e(T._toggle_anim[Q] or A, A, F(.15));
			local N = T._toggle_anim[Q];
			if N > .001 then
				W = W + (z * N);
				local T = (l % 2 == 0);
				if T then
					table.insert(U, {
						name = O,
						h = W,
						tsz = v,
						anim = N,
					});
					TN = (TN + W) + ((v.y / 2 + z)) * N;
				else
					table.insert(q, {
						name = O,
						h = W,
						tsz = v,
						anim = N,
					});
					x = (x + W) + ((v.y / 2 + z)) * N;
				end;
			end;
		end;
		local l = math.max(x, TN);
		local O = (((g + T._tab_h) + z * 3) + l) + 4;
		R = A(O, (g + T._tab_h) + z * 4, T.h);
	end;
	local lN = T:_GetScreenSize();
	if T._active_dropdown then
		local l = T._active_dropdown;
		local O = T._font_size + 9;
		local s = math.min(#((l.choices or {})), 6);
		local v = s * O + z;
		local W = (((l.position.y - E)) + v) + 12;
		if l.position.y + v < lN.y - 10 and W > R then
			R = W;
		end;
	end;
	if T._active_colorpicker and not T._active_colorpicker._closing then
		local l = T._active_colorpicker;
		local O = (((l.position.y - E)) + 215) + 12;
		if l.position.y + 215 < lN.y - 10 and O > R then
			R = O;
		end;
	end;
	local ON = T._current_h or T.h;
	if ON ~= R then
		ON = e(ON, R, F(.22));
		if math.abs(ON - R) < .5 then
			ON = R;
		end;
		T._current_h = ON;
	end;
	local sN = ON;
	local vN = Vector2.new(D - 2, (R - g) - 2);
	local WN = (vN.y - T._tab_h) - z * 2;
	local yN = math.max(0, (math.max(x, TN) - WN) + z);
	if T._menu_open and (P and T._menu_drag) then
		local l = T._cached_mouse or Vector2.new();
		T.x = l.x - T._menu_drag.x;
		T.y = l.y - T._menu_drag.y;
	elseif not P then
		T._menu_drag = nil;
	end;
	local QN = i(T, p);
	local eN = QN.y + 16;
	local AN = QN.x + 44;
	local NN = Vector2.new((lN.x - AN) - 16, 16);
	local tN = ((T._unloading_sequence and T._unload_phase == "menu")) and t(a) or 1;
	if T._watermark_enabled then
		T:_Draw("rect", Q.shadow, 98, .2 * tN, NN + Vector2.new(2, 3), Vector2.new(AN, eN), true, b);
		T:_Draw("rect", Q.surface0, 99, tN, NN, Vector2.new(AN, eN), true, b);
		T:_Draw("rect", Q.border1, 100, tN, NN, Vector2.new(AN, eN), false, b);
		T:_Draw("circle", Q.accent, 101, tN, NN + Vector2.new(12, eN / 2), 3.5, true);
		T:_Draw("text", Q.text, 101, tN, NN + Vector2.new(22, ((eN - T._font_size)) / 2 + 1), p, false, nil, 13);
	end;
	local bN = (16 + eN) + 10;
	for l = #T._notifications, 1, -1 do
		local O = T._notifications[l];
		local s = O._spawned_at + O.time;
		local v = u > s;
		local W = math.max(0, math.min((O._spawned_at - u) + ((v and O.time + 1 or 1)), 1));
		local y = math.abs(((v and 0 or 1)) - ((W * W) * ((3 - 2 * W)))) * tN;
		local e = T:_GetTextBounds(O.text);
		local A = (e.x + z * 2) + 6;
		local N = e.y + z * 1.2;
		local S = ((A + 40)) * ((1 - t(y)));
		local I = Vector2.new(((lN.x - A) - 16) + S, bN);
		local h = math.max(0, math.min(y, 1));
		T:_Draw("rect", Q.shadow, 103, .15 * h, I + Vector2.new(2, 4), Vector2.new(A, N), true, b + 2);
		T:_Draw("rect", Q.surface0, 104, h, I, Vector2.new(A, N), true, b + 2);
		T:_Draw("rect", Q.border1, 105, h, I, Vector2.new(A, N), false, b + 2);
		T:_Draw("text", Q.text, 105, h, I + Vector2.new(z, ((N - T._font_size)) / 2 + 1), O.text, false);
		bN = bN + ((N + 6)) * y;
		if u > s + 1 then
			table.remove(T._notifications, l);
		end;
	end;
	if a <= .015 then
		for l, O in pairs(T._pool or {}) do
			for s = ((T._pIdx[l] or 0)) + 1, #O, 1 do
				local v = O[s];
				local W = T._pool_state[l][s];
				if W and W.Visible ~= false then
					pcall(function()
						v.Visible = false;
					end);
					W.Visible = false;
				end;
			end;
		end;
		return;
	end;
	local SN = ((1 - t(a))) * 50;
	local IN = t(a);
	local hN = false;
	if T._dd_drag or T._slider_drag or T._tab_scroll_drag then
		hN = true;
	end;
	local cN = T._active_dropdown;
	if cN then
		local l = math.min(#((cN.choices or {})), 6) * ((T._font_size + 9)) + z;
		local O = cN.position;
		local s = T:_GetScreenSize();
		if O.y + l > s.y - 10 then
			O = Vector2.new(O.x, (O.y - l) - ((cN._srcH or 0)));
		end;
		if T:_IsMouseWithinBounds(O, Vector2.new(cN.width, l)) then
			hN = true;
		end;
	end;
	local KN = T._active_colorpicker;
	if KN and not KN._closing then
		if T:_IsMouseWithinBounds(KN.position, Vector2.new(240, 215)) then
			hN = true;
		end;
	end;
	if T._ctx_menu then
		hN = true;
	end;
	T:_Draw("rect", Q.shadow, 0, .12 * IN, Vector2.new(H - 1, (E + SN) + 3), Vector2.new(D + 2, sN), true, b + 1);
	T:_Draw("rect", Q.shadow, 0, .06 * IN, Vector2.new(H - 3, (E + SN) + 6), Vector2.new(D + 6, sN), true, b + 3);
	T:_Draw("rect", Q.body, 1, IN, Vector2.new(H, E + SN), Vector2.new(D, sN), true, b);
	T:_Draw("rect", Q.surface0, 20, IN, Vector2.new(H + 1, (E + SN) + 1), Vector2.new(D - 2, g), true, b - 1);
	T:_Draw("rect", Q.border1, 21, IN, Vector2.new(H + 1, (E + SN) + g), Vector2.new(D - 2, 1), true);
	local zN = (E + SN) + g / 2;
	local BN = H + 16;
	local uN = 6;
	T:_Draw("circle", l, 22, IN, Vector2.new(BN, zN), uN, true);
	T:_Draw("circle", s, 22, IN, Vector2.new(BN + 20, zN), uN, true);
	T:_Draw("circle", W, 22, IN, Vector2.new(BN + 40, zN), uN, true);
	T:_Draw("circle", O, 23, IN, Vector2.new(BN, zN), uN, false);
	T:_Draw("circle", v, 23, IN, Vector2.new(BN + 20, zN), uN, false);
	T:_Draw("circle", y, 23, IN, Vector2.new(BN + 40, zN), uN, false);
	if Z and T:_IsMouseWithinBounds(Vector2.new(BN - uN, zN - uN), Vector2.new(uN * 2, uN * 2)) then
		T._menu_open = false;
		T:_RemoveDropdown();
		T:_RemoveColorpicker();
		T:_RemoveCtxMenu();
		T:Notification("Menu Minimized", 3);
		X = false;
		k = true;
	end;
	if T._menu_open and a > .5 then
		T:_Draw("text", Q.text, 22, IN, Vector2.new(H + D / 2, (E + SN) + g / 2), p, false, "center");
	end;
	T._search_query = T._search_query or "";
	T._search_typing = T._search_typing or false;
	T._search_w_anim = T._search_w_anim or 22;
	local YN = ((T._search_typing or T._search_query ~= "")) and 130 or 22;
	T._search_w_anim = e(T._search_w_anim, YN, F(.2));
	local iN = T._search_w_anim;
	local mN = 22;
	local FN = ((H + D) - iN) - 10;
	local oN = (E + SN) + ((g - mN)) / 2;
	local MN = Vector2.new(FN, oN);
	local fN = Vector2.new(iN, mN);
	local wN = not hN and T:_IsMouseWithinBounds(MN, fN);
	if Z and wN then
		T._search_typing = true;
		X = false;
		k = true;
	elseif Z and not k then
		T._search_typing = false;
	end;
	if T._search_typing and T:_IsKeyPressed("esc") then
		T._search_query = "";
		T._search_typing = false;
	end;
	if T._search_typing then
		local l = {
				space = " ",
				dash = "-",
				colon = ":",
				period = ".",
				comma = ",",
				slash = "/",
				semicolon = ";",
				quote = "\'",
				leftbracket = "[",
				rightbracket = "]",
				backslash = "\\",
				equals = "=",
				minus = "-",
			};
		local O = {
				["1"] = "!",
				["2"] = "@",
				["3"] = "#",
				["4"] = "$",
				["5"] = "%",
				["6"] = "^",
				["7"] = "&",
				["8"] = "*",
				["9"] = "(",
				["0"] = ")",
				["-"] = "_",
				["="] = "+",
				["["] = "{",
				["]"] = "}",
				[";"] = ":",
				["\'\'"] = "\"",
				[","] = "<",
				["."] = ">",
				["/"] = "?",
				["\\"] = "|",
			};
		local s = T._search_query;
		local v = T:_IsKeyHeld("lshift") or T:_IsKeyHeld("rshift");
		local W = nil;
		for l in pairs(T._inputs or {}) do
			if T:_IsKeyHeld(l) then
				W = l;
				break;
			end;
		end;
		if W then
			if not T._key_repeat[W] then
				T._key_repeat[W] = os.clock() + .4;
			elseif os.clock() > T._key_repeat[W] then
				T._key_repeat[W] = os.clock() + .05;
				local y = l[W] or W;
				if y == "backspace" then
					s = s:sub(1, -2);
				elseif #y == 1 then
					if v and O[y] then
						y = O[y];
					elseif v then
						y = y:upper();
					end;
					if #s < 24 then
						s = s .. y;
					end;
				end;
				T._search_query = s;
			end;
		else
			T._key_repeat = {};
		end;
		for W in pairs(T._inputs or {}) do
			if T:_IsKeyPressed(W) then
				local y = l[W] or W;
				if y == "enter" then
					T._search_typing = false;
					break;
				elseif y == "backspace" then
					s = s:sub(1, -2);
				elseif #y == 1 then
					if v and O[y] then
						y = O[y];
					elseif v then
						y = y:upper();
					end;
					if #s < 24 then
						s = s .. y;
					end;
				end;
				T._search_query = s;
			end;
		end;
	end;
	local VN = A(((iN - 22)) / (108), 0, 1);
	local JN = N(Q.body, Q.surface1, VN);
	T:_Draw("rect", JN, 22, IN, MN, fN, true, mN / 2);
	T:_Draw("rect", T._search_typing and Q.accent or Q.border1, 23, IN, MN, fN, false, mN / 2);
	local XN = MN.x + 10;
	local PN = (MN.y + fN.y / 2) - 1;
	local LN = N(Q.subtext, Q.text, VN);
	T:_Draw("circle", LN, 24, IN, Vector2.new(XN, PN), 3, false);
	T:_Draw("line", LN, 24, IN, Vector2.new(XN + 2, PN + 2), Vector2.new(XN + 5, PN + 5), 2);
	if VN > .05 then
		local l = T._search_query;
		if T._search_typing then
			l = l .. ((math.floor(os.clock() * 2) % 2 == 0 and "|" or ""));
		end;
		if l == "" and not T._search_typing then
			l = "Global search...";
		end;
		local O = 24;
		if (T:_GetTextBounds(l)).x > (fN.x - O) - 8 then
			for s = 1, #l, 1 do
				local v = l:sub(s);
				if (T:_GetTextBounds(v)).x <= (fN.x - O) - 8 then
					l = v;
					break;
				end;
			end;
		end;
		local s = ((T._search_query ~= "" or T._search_typing)) and Q.text or Q.subtext;
		T:_Draw("text", s, 24, IN * VN, MN + Vector2.new(O, fN.y / 2 - T._font_size / 2), l, false);
	end;
	local rN = Vector2.new(H + 1, (E + g) + 1);
	local nN = Vector2.new(H + 1, ((E + SN) + g) + 1);
	local kN = Vector2.new(D - 2, (sN - g) - 2);
	local jN = (nN.y + T._tab_h) + z;
	local ZN = (kN.y - T._tab_h) - z * 2;
	local dN = jN + ZN;
	local aN = T._scroll_delta;
	T._scroll_delta = 0;
	pcall(function()
		if aN == 0 then
			local T = getmousedelta();
			if T and T.Y ~= 0 then
				aN = T.Y > 0 and 18 or -18;
			end;
		end;
	end);
	if aN ~= 0 then
		if T._active_colorpicker then
			T:_RemoveColorpicker();
		end;
	end;
	local HN = #T._tab_order;
	local EN = 6;
	local DN = kN.x - EN * 2;
	local pN = DN / math.max(1, HN);
	local gN = nN + Vector2.new(EN, EN);
	local GN = rN + Vector2.new(EN, EN);
	T:_Draw("rect", Q.surface1, 20, IN, gN, Vector2.new(DN, T._tab_h), true, b);
	T:_Draw("rect", Q.border1, 21, IN, gN, Vector2.new(DN, T._tab_h), false, b);
	for l, O in ipairs(T._tab_order) do
		local s = gN + Vector2.new(pN * ((l - 1)), 0);
		local v = GN + Vector2.new(pN * ((l - 1)), 0);
		local W = Vector2.new(pN, T._tab_h);
		local y = T._open_tab == O;
		local e = T._search_tab_results[O] or 0;
		local A = y and Q.text or Q.subtext;
		T:_Draw("text", A, 24, IN, s + Vector2.new(pN / 2, T._tab_h / 2), O, false, "center");
		if T._search_query ~= "" and (e > 0 and not y) then
			local l = T:_GetTextBounds(O, nil, T._font_size);
			local v = ((s.x + pN / 2) + l.x / 2) + 6;
			local W = (s.y + T._tab_h / 2) - 4;
			T:_Draw("circle", Q.accent, 25, IN, Vector2.new(v, W), 3, true);
		end;
		if not y and (not hN and (T:_IsMouseWithinBounds(v, W) and (X and not k))) then
			if not T._tab_changing then
				T._tab_changing = true;
				T._next_tab = O;
				T._input_ctx = nil;
				T:_RemoveDropdown();
				T:_RemoveColorpicker();
				T:_RemoveCtxMenu();
				T._pill_target_x = pN * ((l - 1));
				X = false;
				k = true;
			end;
		end;
	end;
	T._pill_x = e(T._pill_x, T._pill_target_x, F(.16));
	local RN = gN.x + T._pill_x;
	T:_Draw("rect", Q.surface0, 22, IN, Vector2.new(RN + 3, gN.y + 3), Vector2.new(pN - 6, T._tab_h - 6), true, b - 2);
	T:_Draw("rect", Q.border0, 23, IN, Vector2.new(RN + 3, gN.y + 3), Vector2.new(pN - 6, T._tab_h - 6), false, b - 2);
	for l, O in ipairs(T._tab_order) do
		local s = T._tree[O];
		if s and (T._open_tab == O and (T._menu_open and a > .5)) then
			T._tab_scroll[O] = T._tab_scroll[O] or { offset = 0, target = 0, maxScroll = 0 };
			local l = T._tab_scroll[O];
			local s = math.max(x, TN);
			l.maxScroll = math.max(0, (s - ZN) + z);
			local v = yN > 0 and (l.maxScroll > 0 and f > .75);
			if v then
				local s = 3;
				local v = ((nN.x + kN.x) - s) - 2;
				local W = ZN;
				local y = math.max(20, W * ((W / ((W + l.maxScroll)))));
				local Q = Vector2.new(s + 16, W);
				local e = Vector2.new(v - 8, jN);
				local N = not hN and T:_IsMouseWithinBounds(e, Q);
				if P then
					if ((N or T._tab_scroll_drag == O)) and (X and not k) then
						T._tab_scroll_drag = O;
						X = false;
						k = true;
					end;
					if T._tab_scroll_drag == O then
						local O = T:_GetMousePos();
						local s = A((((O.y - jN) - y / 2)) / math.max(1, W - y), 0, 1);
						l.target = s * l.maxScroll;
					end;
				else
					if T._tab_scroll_drag == O then
						T._tab_scroll_drag = nil;
					end;
				end;
			end;
		end;
	end;
	for l, O in ipairs(T._tab_order) do
		local s = T._tree[O];
		if s and T._open_tab == O then
			T._tab_scroll[O] = T._tab_scroll[O] or { offset = 0, target = 0, maxScroll = 0 };
			local l = T._tab_scroll[O];
			local v = T._menu_open and a > .5;
			local W = kN.x / 2 - z * 1.5;
			local y = math.max(x, TN);
			local I = math.max(0, (y - ZN) + z);
			l.maxScroll = I;
			if v then
				local O = yN > 0 and (I > 0 and f > .75);
				if O then
					local O = 3;
					local s = ((nN.x + kN.x) - O) - 2;
					local v = ZN;
					local W = math.max(20, v * ((v / ((v + I)))));
					local y = l.offset;
					local e = jN + ((v - W)) * ((y / math.max(1, I)));
					if aN ~= 0 and (not hN and T:_IsMouseWithinBounds(nN, kN)) then
						l.target = A(l.target - aN, 0, I);
					end;
					local N = IN * A(((f - .75)) / .25, 0, 1);
					T:_Draw("rect", Q.border0, 13, .35 * N, Vector2.new(s, jN), Vector2.new(O, v), true, O / 2);
					T:_Draw("rect", Q.subtext, 14, .7 * N, Vector2.new(s, e), Vector2.new(O, W), true, O / 2);
				elseif aN ~= 0 and (not hN and T:_IsMouseWithinBounds(nN, kN)) then
					l.target = A(l.target - aN, 0, I);
				end;
			end;
			if l.offset ~= l.target then
				l.offset = e(l.offset, l.target, F(.18));
				if math.abs(l.offset - l.target) < .5 then
					l.offset = l.target;
				end;
			end;
			local h = l.offset;
			local function c(l, y, I)
				local c = (jN + z) - h;
				for l, y in ipairs(l) do
					local h = y.name;
					local K = y.h;
					local u = y.tsz;
					local Y = y.anim;
					local m = "menu_sec_" .. (O .. ("_" .. h));
					T._sec_slide = T._sec_slide or {};
					if T._sec_slide[m] == nil then
						T._sec_slide[m] = 10;
					end;
					local M = T._sec_slide[m];
					if M ~= 0 then
						M = e(M, 0, F(.14));
						if math.abs(M) < .25 then
							M = 0;
						end;
						T._sec_slide[m] = M;
					end;
					local w = M;
					local V = (c + ((u.y / 2)) * Y) + w;
					local J = Vector2.new(I, V);
					if v then
						local l = J.y;
						local v = J.y + K;
						local y = math.max(l, jN);
						local I = math.min(v, dN);
						local m = math.max(0, I - y);
						local M = m > 0;
						local w = b;
						local V = Y * f;
						if T._unloading_sequence and T._unload_phase == "sections" then
							V = V * ((T._unload_section_alpha or 1));
						end;
						if M and V > .001 then
							T:_Draw("rect", Q.shadow, 2, (.1 * V) * IN, Vector2.new(J.x + 2, y + 3), Vector2.new(W, m), true, w);
							T:_Draw("rect", Q.surface0, 3, V * IN, Vector2.new(J.x, y), Vector2.new(W, m), true, w);
							T:_Draw("rect", Q.border1, 4, V * IN, Vector2.new(J.x, y), Vector2.new(W, m), false, w);
						end;
						local r = (J.y - ((u.y / 2)) * Y) - 7;
						local n = r + u.y;
						local j = 12;
						local Z = Y * f;
						local d = (n > jN) and (r < dN);
						if d then
							local l = r - jN;
							local O = dN - n;
							if l < j then
								Z = math.min(Z, math.max(0, l / j));
							end;
							if O < j then
								Z = math.min(Z, math.max(0, O / j));
							end;
							if l < 0 or O < 0 then
								Z = 0;
							end;
							if T._unloading_sequence and T._unload_phase == "sections" then
								Z = Z * ((T._unload_section_alpha or 1));
							end;
						else
							Z = 0;
						end;
						if Z > .001 then
							T:_Draw("text", Q.subtext, 6, IN * Z, Vector2.new(J.x + z, r), h, false);
						end;
						local a = s._items and s._items[h] or { _items = {} };
						local H = ((z + u.y / 2)) * Y;
						for l, s in ipairs(a._items or {}) do
							local v = s._anim_key;
							local y = T._toggle_anim[v] or 0;
							if y > .001 then
								local v = o(T, s);
								local b = v * y;
								local I = Vector2.new(J.x + z, J.y + H);
								local c = s.type_;
								local K = s.value;
								local u = s.callback;
								local m = O .. (h .. tostring(l));
								local w = I.y;
								local V = (I.y + b) - (z * y);
								local r = M and ((V >= jN) and (w <= dN));
								local n = y * f;
								if r then
									local l = w - jN;
									local O = dN - V;
									if l < j then
										n = math.min(n, math.max(0, l / j));
									end;
									if O < j then
										n = math.min(n, math.max(0, O / j));
									end;
									if l < 0 or O < 0 then
										n = 0;
									end;
									if T._unloading_sequence and T._unload_phase == "sections" then
										n = n * ((T._unload_section_alpha or 1));
									end;
								end;
								if n <= .001 then
									r = false;
								end;
								local Z = s._id;
								local d = r and (n > .4);
								if not r then
									T._item_vis[Z] = false;
								else
									T._item_vis[Z] = true;
									local l = I;
									local O = d and (X and not k);
									local y = (IN * n) * Y;
									if c == "toggle" then
										local v = K and 1 or 0;
										local A = T._toggle_anim[m];
										if A == nil then
											A = v;
										elseif A ~= v then
											A = e(A, v, F(.1));
										end;
										T._toggle_anim[m] = A;
										local t = 34;
										local b = T._font_size + 2;
										local S = s.keybind;
										local I = s.colorpicker;
										local h = 0;
										if S then
											h = B + z;
											local v = ((S.value or "?")):upper();
											local e = S._listening and "..." or v;
											local A = 12;
											while A > 7 and (T:_GetTextBounds(e, nil, A)).x > B - 6 do
												A = A - 1;
											end;
											local t = Vector2.new(((J.x + W) - B) - z, l.y + ((b - T._font_size)) / 2);
											local I = Vector2.new(B, T._font_size + 2);
											local c = not hN and T:_IsMouseWithinBounds(t, I);
											local K = c and N(Q.surface1, Q.accent, .2) or Q.surface1;
											if S.value and not S._listening then
												if T:_IsKeyPressed(S.value) then
													if not s.value then
														T:Notification("Enable " .. (s.label .. " first"), 3);
													else
														local T = not s.value;
														s.value = T;
														if u then
															u(T);
														end;
													end;
												end;
											end;
											T:_Draw("rect", Q.shadow, 6, .1 * y, t + Vector2.new(1, 2), I, true, 6);
											T:_Draw("rect", K, 7, y, t, I, true, 6);
											T:_Draw("rect", S._listening and Q.accent or Q.border1, 8, y, t, I, false, 6);
											local Y = ((c or S._listening)) and Q.text or Q.subtext;
											T:_Draw("text", Y, 8, y, t + Vector2.new(B / 2, I.y / 2), e, false, "center", A);
											if c and O then
												S._listening = true;
												S._listening_start = os.clock();
												X = false;
												k = true;
											end;
											if S._listening then
												local l = os.clock() - S._listening_start;
												if l > 6 then
													S._listening = false;
												elseif l > .15 then
													for l, O in pairs(T._inputs or {}) do
														if T:_IsKeyPressed(l) then
															if l == "m1" then
																S._listening = false;
															else
																local v = l ~= "backspace" and l;
																if v then
																	for l, O in ipairs(T._config_refs or {}) do
																		local W = O.item;
																		if W ~= s and (W.keybind and W.keybind.value == v) then
																			T:Notification("Conflict: " .. (v:upper() .. (" used by " .. W.label)), 4);
																		end;
																	end;
																end;
																if S.callback and T._inputs[v] then
																	S.callback(O.id);
																end;
																S.value = v;
																S._listening = false;
															end;
														end;
													end;
												end;
											end;
										elseif I then
											local s = Vector2.new(24, 14);
											local v = l + Vector2.new((W - z * 2) - s.x, ((b - s.y)) / 2);
											h = s.x + z * 2;
											local e = not hN and T:_IsMouseWithinBounds(v, s);
											T:_Draw("rect", Q.border1, 7, y, v, s, true, 4);
											T:_Draw("rect", I.value, 8, y, v + Vector2.new(1, 1), s - Vector2.new(2, 2), true, 4);
											if e and O then
												T:_SpawnColorpicker(nil, I.label, I.value, function(T)
													I.value = T;
													if I.callback then
														I.callback(T);
													end;
												end);
												X = false;
												k = true;
											end;
										end;
										local c = s.unsafe and Q.unsafe or (K and Q.text or Q.subtext);
										if not I or not I.overwrite then
											local v = not hN and T:_IsMouseWithinBounds(l, Vector2.new(t, b));
											if v and O then
												local T = not K;
												s.value = T;
												if u then
													u(T);
												end;
												X = false;
												k = true;
											end;
											local W = N(Q.surface1, Q.accent, A);
											T:_Draw("rect", W, 7, y, l, Vector2.new(t, b), true, b / 2);
											T:_Draw("rect", N(Q.border1, Q.accent, A), 8, y, l, Vector2.new(t, b), false, b / 2);
											local e = (b / 2) - 1;
											local S = ((l.x + e) + 1) + ((t - b)) * A;
											local I = l.y + b / 2;
											T:_Draw("circle", Q.shadow, 8, .15 * y, Vector2.new(S, I + 1), e, true);
											T:_Draw("circle", Q.surface0, 9, y, Vector2.new(S, I), e, true);
										else
											c = Q.text;
										end;
										local Y = s.label;
										local i = false;
										local o = (((W - t) - z * 2) - h) - ((s.isNew and 36 or 0));
										while #Y > 1 and (T:_GetTextBounds(Y)).x > o do
											Y = Y:sub(1, -2);
											i = true;
										end;
										if i then
											Y = Y:sub(1, -2) .. "..";
										end;
										local M = T:_GetTextBounds(Y);
										local f = l + Vector2.new(t + z, ((b - T._font_size)) / 2);
										T:_Draw("text", c, 7, y, f, Y, false);
										if d and (i and (not hN and T:_IsMouseWithinBounds(f, M))) then
											T._frame_tooltip = { key = Z, text = s.label };
										end;
										if s.isNew then
											local l = f + Vector2.new(M.x + 8, -2);
											T:_Draw("rect", Q.accent, 8, y, l, Vector2.new(28, 14), true, 4);
											T:_Draw("text", Q.surface0, 9, y, l + Vector2.new(14, 7), "NEW", false, "center", 9);
										end;
									elseif c == "slider" then
										local e = s.min or 0;
										local t = s.max or 100;
										local b = s.step or 1;
										local I = T:_GetTextBounds(s.label);
										local h = T._font_size;
										local c = (l.x + h) + z;
										local B = ((W - z * 2) - h * 2) - z * 2;
										local Y = Vector2.new(c, (l.y + I.y) + z);
										local i = Vector2.new(B, 6);
										local m = s.label;
										local F = false;
										local o = (W - z * 2) - ((s.isNew and 36 or 0));
										while #m > 1 and (T:_GetTextBounds(m)).x > o do
											m = m:sub(1, -2);
											F = true;
										end;
										if F then
											m = m:sub(1, -2) .. "..";
										end;
										local M = T:_GetTextBounds(m);
										T:_Draw("text", Q.subtext, 7, y, l, m, false);
										if d and (F and (not hN and T:_IsMouseWithinBounds(l, M))) then
											T._frame_tooltip = { key = Z, text = s.label };
										end;
										if s.isNew then
											local O = l + Vector2.new(M.x + 8, 0);
											T:_Draw("rect", Q.accent, 8, y, O, Vector2.new(28, 14), true, 4);
											T:_Draw("text", Q.surface0, 9, y, O + Vector2.new(14, 7), "NEW", false, "center", 9);
										end;
										local f = K or e;
										local w = Y - Vector2.new(h + z, i.y + z);
										local V = Vector2.new(B + ((h + z)) * 2, v);
										local J = not hN and T:_IsMouseWithinBounds(w, V);
										if J and (L and d) then
											f = s.default ~= nil and s.default or e;
											s.value = f;
											if u then
												u(f);
											end;
											T:Notification("Reset to default", 2);
											L = false;
										end;
										local r = not hN and T:_IsMouseWithinBounds(Y - Vector2.new(0, 6), i + Vector2.new(0, 12));
										if P then
											if d and (((r or T._slider_drag == Z)) and (X and T._slider_drag ~= Z)) then
												T._slider_drag = Z;
												X = false;
												k = true;
											end;
											if T._slider_drag == Z then
												local l = A((((T:_GetMousePos()).x - Y.x)) / i.x, 0, 1);
												f = e + ((t - e)) * l;
												f = math.floor((f / b) + .5) * b;
												f = A(f, e, t);
											end;
										else
											T._slider_drag = nil;
										end;
										local n = l.x + h / 2;
										local j = ((c + B) + z) + h / 2;
										local a = Y.y + i.y / 2;
										local H = Vector2.new(l.x, a - h / 2);
										local E = Vector2.new((c + B) + z, a - h / 2);
										local D = Vector2.new(h, h);
										local p = not hN and T:_IsMouseWithinBounds(H, D);
										local g = not hN and T:_IsMouseWithinBounds(E, D);
										T:_Draw("text", p and Q.text or Q.subtext, 7, y, Vector2.new(n, a), "-", false, "center");
										T:_Draw("text", g and Q.text or Q.subtext, 7, y, Vector2.new(j, a), "+", false, "center");
										if O and not hN then
											if p then
												f = A(f - b, e, t);
												X = false;
												k = true;
											elseif g then
												f = A(f + b, e, t);
												X = false;
												k = true;
											end;
										end;
										if f ~= K then
											s.value = f;
											if u then
												u(f);
											end;
										end;
										local G = A(((f - e)) / (math.max(.01, t - e)), 0, 1);
										local R = Z .. "_dv";
										T._slider_disp[R] = f;
										local C = (T._slider_drag == Z);
										local q = ((r or C)) and N(Q.surface1, Q.accent, .12) or Q.surface1;
										T:_Draw("rect", q, 7, y, Y, i, true, 3);
										T:_Draw("rect", ((r or C)) and Q.border0 or Q.border1, 8, y, Y, i, false, 3);
										local U = math.max(0, i.x * G - 2);
										T:_Draw("rect", Q.accent, 9, y, Y + Vector2.new(1, 1), Vector2.new(U, i.y - 2), true, 3);
										local x = Y.x + i.x * G;
										local TN = Y.y + i.y / 2;
										local lN = C and 7 or 5;
										T:_Draw("circle", Q.surface0, 10, y, Vector2.new(x, TN), lN, true);
										T:_Draw("circle", Q.accent, 11, y, Vector2.new(x, TN), lN, false);
										local ON = S(f) .. s.suffix;
										T:_Draw("text", Q.text, 15, y, Vector2.new(x, (TN + lN) + 6), ON, false, "center", 11);
									elseif c == "dropdown" then
										local v = T:_GetTextBounds(s.label);
										local A = Vector2.new(l.x, (l.y + v.y) + z);
										local t = Vector2.new(W - z * 2, v.y + z);
										local b = not hN and T:_IsMouseWithinBounds(A, t);
										local S = T._active_dropdown and T._active_dropdown_owner == Z;
										if S then
											T._active_dropdown.position = Vector2.new(A.x, A.y + t.y);
											T._active_dropdown._srcH = t.y;
										end;
										if b and O then
											if S then
												T:_RemoveDropdown();
												T._active_dropdown_owner = nil;
											else
												T:_SpawnDropdown(Vector2.new(A.x, A.y + t.y), t.x, K, s.choices, s.multi, function(T)
													s.value = T;
													if u then
														u(T);
													end;
												end);
												T._active_dropdown._srcH = t.y;
												T._active_dropdown_owner = Z;
											end;
											X = false;
											k = true;
										end;
										local I = Z .. "_arw";
										T._dd_arrow_anim[I] = e(T._dd_arrow_anim[I] or 0, S and 1 or 0, F(.25));
										local h = T._dd_arrow_anim[I];
										local c = math.floor(((A.x + t.x) - z) - 4);
										local B = math.floor(A.y + t.y / 2);
										local Y = Vector2.new(math.floor(e(c + 2, c - 3, h) + .5), math.floor(e(B - 3, B - 2, h) + .5));
										local m = Vector2.new(math.floor(e(c + 2, c + 3, h) + .5), math.floor(e(B + 3, B - 2, h) + .5));
										local o = Vector2.new(math.floor(e(c - 3, c, h) + .5), math.floor(e(B, B + 3, h) + .5));
										local M = b and N(Q.surface0, Q.accent, .06) or Q.surface0;
										T:_Draw("rect", Q.shadow, 6, .1 * y, A + Vector2.new(1, 2), t, true, 6);
										T:_Draw("rect", M, 7, y, A, t, true, 6);
										T:_Draw("rect", Q.border1, 8, y, A, t, false, 6);
										T:_Draw("triangle", Q.subtext, 8, y, true, Y, m, o);
										local f = type(K) == "table" and table.concat(K, ", ") or tostring(K or "");
										local w = false;
										if f == "" then
											f = ((s.choices and s.choices[1])) and tostring(s.choices[1]) or "";
											w = true;
										end;
										if (T:_GetTextBounds(f)).x > (t.x - z) - 14 then
											if w then
												local l = (t.x - z) - 26;
												while #f > 1 and (T:_GetTextBounds(f)).x > l do
													f = f:sub(1, -2);
												end;
												f = f .. "..";
											else
												local T = type(K) == "table" and #((K or {})) or 1;
												f = tostring(T) .. (" item" .. ((T == 1 and "" or "s")));
											end;
										end;
										local V = w and Q.subtext or Q.text;
										T:_Draw("text", V, 8, y, A + Vector2.new(6, (T:_GetTextBounds(f)).y / 2 - 1), f, false);
										local J = (W - z * 2) - ((s.isNew and 36 or 0));
										local P = s.label;
										local L = false;
										local r = (i(T, P)).x;
										if r > J then
											local T = J / math.max(1, r);
											P = P:sub(1, math.max(1, math.floor(#P * T) - 2)) .. "..";
											L = true;
										end;
										local n = i(T, P);
										T:_Draw("text", Q.subtext, 7, y, l + Vector2.new(z, 0), P, false);
										if d and (L and (not hN and T:_IsMouseWithinBounds(l + Vector2.new(z, 0), n))) then
											T._frame_tooltip = { key = Z .. "dd", text = s.label };
										end;
										if s.isNew then
											local O = l + Vector2.new((z + n.x) + 8, -2);
											T:_Draw("rect", Q.accent, 8, y, O, Vector2.new(28, 14), true, 4);
											T:_Draw("text", Q.surface0, 9, y, O + Vector2.new(14, 7), "NEW", false, "center", 9);
										end;
									elseif c == "button" then
										local v = s.colorpicker;
										local A = 0;
										local b = (T:_GetTextBounds(s.label)).y + z;
										if v then
											local s = Vector2.new(24, 14);
											local e = l + Vector2.new((W - z * 2) - s.x, ((b - s.y)) / 2);
											A = s.x + z * 2;
											local N = not hN and T:_IsMouseWithinBounds(e, s);
											T:_Draw("rect", Q.border1, 7, y, e, s, true, 4);
											T:_Draw("rect", v.value, 8, y, e + Vector2.new(1, 1), s - Vector2.new(2, 2), true, 4);
											if N and O then
												T:_SpawnColorpicker(nil, v.label, v.value, function(T)
													v.value = T;
													if v.callback then
														v.callback(T);
													end;
												end);
												X = false;
												k = true;
											end;
										end;
										local S = Vector2.new(l.x, l.y);
										local I = Vector2.new((W - z * 2) - A, b);
										local h = not hN and T:_IsMouseWithinBounds(S, I);
										if P then
											if h and O then
												T._slider_drag = Z;
												X = false;
												k = true;
												if u then
													u();
												end;
											end;
										else
											T._slider_drag = nil;
										end;
										local c = P and T._slider_drag == Z;
										local K = Z .. "_bp";
										T._toggle_anim[K] = e(T._toggle_anim[K] or 0, c and 1 or 0, F(.45));
										local B = T._toggle_anim[K];
										local Y = math.floor(B * 2 + .5);
										local i = S + Vector2.new(Y, Y);
										local m = I - Vector2.new(Y * 2, Y * 2);
										local o = N(h and N(Q.surface0, Q.accent, .08) or Q.surface0, Q.accent, B * .85);
										T:_Draw("rect", Q.shadow, 6, (((1 - B)) * .1) * y, S + Vector2.new(1, 2), I, true, 6);
										T:_Draw("rect", o, 7, y, i, m, true, 6);
										T:_Draw("rect", c and Q.accent or Q.border1, 8, y, i, m, false, 6);
										local M = ((c or h)) and Q.text or Q.subtext;
										if s._last_label == nil then
											s._last_label = s.label;
										end;
										local f = Z .. "_lbl_anim";
										if s.label ~= s._last_label then
											s._old_label = s._last_label;
											s._last_label = s.label;
											T._toggle_anim[f] = 0;
										end;
										local w = T._toggle_anim[f] or 1;
										w = e(w, 1, F(.35));
										T._toggle_anim[f] = w;
										if w < .995 and s._old_label then
											local l = t(w);
											local O = y * ((1 - l));
											local v = (i.y + m.y / 2) - (10 * l);
											T:_Draw("text", M, 8, O, Vector2.new(i.x + m.x / 2, v), s._old_label, false, "center");
											local W = y * l;
											local Q = (i.y + m.y / 2) + 10 * ((1 - l));
											T:_Draw("text", M, 8, W, Vector2.new(i.x + m.x / 2, Q), s.label, false, "center");
										else
											T:_Draw("text", M, 8, y, i + Vector2.new(m.x / 2, m.y / 2), s.label, false, "center");
										end;
										if s.isNew then
											local l = T:_GetTextBounds(s.label);
											local O = i + Vector2.new((m.x / 2 + l.x / 2) + 8, ((m.y - 14)) / 2);
											T:_Draw("rect", Q.accent, 9, y, O, Vector2.new(28, 14), true, 4);
											T:_Draw("text", Q.surface0, 10, y, O + Vector2.new(14, 7), "NEW", false, "center", 9);
										end;
									elseif c == "textbox" then
										local v = l;
										local A = Vector2.new(W - z * 2, T._font_size + z);
										local t = not hN and T:_IsMouseWithinBounds(v, A);
										local b = T._input_ctx == Z;
										local S = math.floor(os.clock() * 2) % 2 == 0 and "|" or " ";
										local I = K and K ~= "";
										local h = Z .. "_ph";
										T._toggle_anim[h] = e(T._toggle_anim[h] or (I and 1 or 0), I and 1 or 0, F(.18));
										local c = T._toggle_anim[h];
										local B = b and (((K or "")) .. S) or ((I and K or s.label)) .. " ";
										local u = ((b or t)) and Q.text or N(Q.subtext, Q.text, c);
										if (T:_GetTextBounds(B)).x > A.x then
											local l, O, s = 1, #B, #B;
											while l < O do
												local s = math.floor(((l + O)) / 2);
												if (T:_GetTextBounds(B:sub(s))).x <= A.x - 4 then
													O = s;
												else
													l = s + 1;
												end;
											end;
											B = B:sub(l);
										end;
										local Y = T:_GetTextBounds(B);
										if T:_IsKeyPressed("m1") then
											if O and t then
												T._input_ctx = Z;
												T:_RemoveCtxMenu();
												X = false;
												k = true;
											elseif b then
												T._input_ctx = nil;
												T:_RemoveDropdown();
												b = false;
											end;
										end;
										if L and (d and t) then
											local l = T:_GetMousePos();
											T:_SpawnCtxMenu(l.x, l.y, Z, s);
											L = false;
										end;
										if b then
											local l = {
													space = " ",
													dash = "-",
													colon = ":",
													period = ".",
													comma = ",",
													slash = "/",
													semicolon = ";",
													quote = "\'",
													leftbracket = "[",
													rightbracket = "]",
													backslash = "\\",
													equals = "=",
													minus = "-",
												};
											local O = {
													["1"] = "!",
													["2"] = "@",
													["3"] = "#",
													["4"] = "$",
													["5"] = "%",
													["6"] = "^",
													["7"] = "&",
													["8"] = "*",
													["9"] = "(",
													["0"] = ")",
													["-"] = "_",
													["="] = "+",
													["["] = "{",
													["]"] = "}",
													[";"] = ":",
													["\'"] = "\"",
													[","] = "<",
													["."] = ">",
													["/"] = "?",
													["\\"] = "|",
												};
											local v = K or "";
											local W = T:_IsKeyHeld("lshift") or T:_IsKeyHeld("rshift");
											local y = nil;
											for l in pairs(T._inputs or {}) do
												if T:_IsKeyHeld(l) then
													y = l;
													break;
												end;
											end;
											if y then
												if not T._key_repeat[y] then
													T._key_repeat[y] = os.clock() + .4;
												elseif os.clock() > T._key_repeat[y] then
													T._key_repeat[y] = os.clock() + .05;
													local Q = l[y] or y;
													if Q == "backspace" then
														v = v:sub(1, -2);
													elseif #Q == 1 then
														if W and O[Q] then
															Q = O[Q];
														elseif W then
															Q = Q:upper();
														end;
														if #v < 200 then
															v = v .. Q;
														end;
													end;
													if s.callback then
														s.callback(v);
													end;
													s.value = v;
												end;
											else
												T._key_repeat = {};
											end;
											for y in pairs(T._inputs or {}) do
												if T:_IsKeyPressed(y) then
													local Q = l[y] or y;
													if Q == "enter" or Q == "esc" then
														T._input_ctx = nil;
														break;
													elseif Q == "backspace" then
														v = v:sub(1, -2);
													elseif #Q == 1 then
														if W and O[Q] then
															Q = O[Q];
														elseif W then
															Q = Q:upper();
														end;
														if #v < 200 then
															v = v .. Q;
														end;
													end;
													if s.callback then
														s.callback(v);
													end;
													s.value = v;
												end;
											end;
										end;
										T:_Draw("rect", b and Q.surface0 or Q.surface1, 7, y, v, A, true, 6);
										T:_Draw("rect", b and Q.accent or (t and Q.border0 or Q.border1), 8, y, v, A, false, 6);
										T:_Draw("text", u, 8, y, v + Vector2.new(6, Y.y / 2 - 2), B, false);
									end;
								end;
								H = H + b;
							end;
						end;
						c = (c + K) + ((u.y / 2 + z)) * Y;
					end;
				end;
			end;
			local K = kN.x / 2 - z * 1.5;
			c(q, false, rN.x + z);
			c(U, true, (rN.x + z * 2) + K);
		end;
	end;
	local CN = T._ctx_menu;
	if CN then
		CN._anim = e(CN._anim or 0, 1, F(.3));
		local l = CN._anim;
		local O = T._font_size + 8;
		local s = 80;
		local v = #CN.items * O + 4;
		local W = CN.x;
		local y = CN.y;
		if W + s > (H + D) - 4 then
			W = ((H + D) - s) - 4;
		end;
		if y + v > (E + sN) - 4 then
			y = y - v;
		end;
		local A = Vector2.new(W, y);
		local t = Vector2.new(s, v);
		T:_Draw("rect", Q.shadow, 700, (.18 * l) * IN, A + Vector2.new(2, 4), t, true, 6);
		T:_Draw("rect", Q.surface0, 701, l * IN, A, t, true, 6);
		T:_Draw("rect", Q.border1, 702, l * IN, A, t, false, 6);
		for v, e in ipairs(CN.items) do
			local A = (y + 2) + ((v - 1)) * O;
			local t = Vector2.new(W + 2, A);
			local b = Vector2.new(s - 4, O);
			local S = T:_IsMouseWithinBounds(t, b);
			if S then
				T:_Draw("rect", N(Q.surface0, Q.accent, .18), 703, l * IN, t, b, true, 4);
			end;
			T:_Draw("text", S and Q.text or Q.subtext, 704, l * IN, t + Vector2.new(s / 2 - 2, O / 2), e.label, false, "center", 12);
			if S and (X and not k) then
				local l = CN.sItem;
				if e.action == "copy" then
					local O = l.value or "";
					if type(setclipboard) == "function" then
						pcall(setclipboard, O);
					end;
					T:Notification("Copied to clipboard", 2);
				elseif e.action == "clear" then
					l.value = "";
					if l.callback then
						l.callback("");
					end;
					T:_MarkDirty();
					T:Notification("Cleared", 2);
				end;
				T:_RemoveCtxMenu();
				X = false;
				k = true;
			end;
		end;
		if ((X or L)) and (not k and not T:_IsMouseWithinBounds(A, t)) then
			T:_RemoveCtxMenu();
			X = false;
			L = false;
			k = true;
		end;
	end;
	local qN = T._active_dropdown;
	if qN then
		qN._anim = e(qN._anim or 0, 1, F(.15));
		local l = qN._anim;
		local O = qN.position;
		local s = T._font_size + 9;
		local v = 6;
		local W = #((qN.choices or {}));
		local y = math.min(W, v);
		local b = qN.width;
		local S = y * s + z;
		local I = T:_GetScreenSize();
		local h = false;
		if O.y + S > I.y - 10 then
			h = true;
			O = Vector2.new(O.x, (O.y - S) - ((qN._srcH or 0)));
		end;
		local c = W > v;
		local K = math.max(0, W * s - ((S - z)));
		if aN ~= 0 and T:_IsMouseWithinBounds(O, Vector2.new(b, S)) then
			qN._scroll_target = A(((qN._scroll_target or 0)) - aN, 0, K);
		end;
		qN._scroll_target = qN._scroll_target or 0;
		qN._scroll_v = e(qN._scroll_v or 0, qN._scroll_target, F(.25));
		if math.abs(qN._scroll_v - qN._scroll_target) < .5 then
			qN._scroll_v = qN._scroll_target;
		end;
		local B = math.max(1, S * t(l));
		local u = h and Vector2.new(O.x, O.y + ((S - B))) or O;
		T:_Draw("rect", Q.shadow, 500, .15 * IN, u + Vector2.new(2, 4), Vector2.new(b, B), true, 6);
		T:_Draw("rect", Q.surface0, 501, IN, u, Vector2.new(b, B), true, 6);
		T:_Draw("rect", Q.border1, 502, IN, u, Vector2.new(b, B), false, 6);
		local Y = u.y;
		local i = u.y + B;
		local m, o, M;
		if c and B > 20 then
			local l = 3;
			o = (b - l) - 2;
			M = B - z;
			m = math.max(10, M * ((M / ((M + K)))));
			if P then
				if T:_IsMouseWithinBounds(u + Vector2.new(o - 5, z / 2), Vector2.new(l + 10, M)) and (X and not k) then
					T._dd_drag = "dd_sb";
					X = false;
					k = true;
				end;
				if T._dd_drag == "dd_sb" then
					local l = T:_GetMousePos();
					local O = A((((l.y - ((u.y + z / 2))) - m / 2)) / math.max(1, M - m), 0, 1);
					qN._scroll_target = O * K;
				end;
			else
				T._dd_drag = nil;
			end;
			local O = z / 2 + ((M - m)) * ((qN._scroll_v / K));
			T:_Draw("rect", Q.border0, 503, .35 * IN, u + Vector2.new(o, z / 2), Vector2.new(3, M), true, 1.5);
			T:_Draw("rect", Q.subtext, 504, .7 * IN, u + Vector2.new(o, O), Vector2.new(3, m), true, 1.5);
		end;
		for l, v in ipairs(qN.choices or {}) do
			local W = ((((h and O.y or O.y)) + z / 2) + ((l - 1)) * s) - qN._scroll_v;
			local y = W + s;
			if y > Y and W < i then
				local l = 1;
				local e = 3;
				local A = W - Y;
				local t = i - y;
				if A < e then
					l = math.max(0, A / e);
				end;
				if t < e then
					l = math.min(l, math.max(0, t / e));
				end;
				if A < 0 or t < 0 then
					l = 0;
				end;
				if l > .05 then
					local y = type(qN.value) == "table" and table.find(qN.value, v) or false;
					local e = Vector2.new(O.x + 6, W);
					local A = Vector2.new(b - ((c and 16 or 12)), s);
					local t = (l > .4);
					local S = t and T:_IsMouseWithinBounds(e, A);
					if S then
						local O = math.max(e.y + 1, Y + 2);
						local s = math.min((e.y + A.y) - 1, i - 2);
						local v = math.max(0, s - O);
						if v > 0 then
							T:_Draw("rect", N(Q.surface0, Q.accent, .18), 505, IN * l, Vector2.new(e.x + 2, O), Vector2.new(A.x - 4, v), true, 4);
						end;
					end;
					local I = S and Q.text or (y and Q.text or Q.subtext);
					T:_Draw("text", I, 506, IN * l, e + Vector2.new(2, 4), v, false, nil, 12);
					if S and (X and not k) then
						if qN.multi then
							if type(qN.value) ~= "table" then
								qN.value = {};
							end;
							local T = table.find(qN.value, v);
							if T then
								table.remove(qN.value, T);
							else
								table.insert(qN.value, v);
							end;
						else
							qN.value = { v };
						end;
						if qN.callback then
							qN.callback(qN.value);
						end;
						if not qN.multi then
							T:_RemoveDropdown();
						end;
						X = false;
						k = true;
						break;
					end;
				end;
			end;
		end;
		if X and (not k and not T:_IsMouseWithinBounds(u, Vector2.new(b, B))) then
			T:_RemoveDropdown();
			T._active_dropdown_owner = nil;
			X = false;
			k = true;
		end;
	end;
	local UN = T._active_colorpicker;
	if UN then
		UN._anim = e(UN._anim or 0, UN._closing and 0 or 1, F(.15));
		local l = UN._anim;
		if UN._closing and l < .05 then
			T._active_colorpicker = nil;
		else
			local O = true;
			local s = Vector2.new(240, 215);
			local v = UN.position;
			local W = T:_GetTextBounds(UN.label);
			local y = Vector2.new(s.x, math.max(1, s.y * t(l)));
			T:_Draw("rect", Q.shadow, 600, .18 * IN, v + Vector2.new(2, 4), y, true, b);
			T:_Draw("rect", Q.surface0, 601, IN, v, y, true, b);
			T:_Draw("rect", Q.border1, 602, IN, v, y, false, b);
			if y.y > 20 then
				T:_Draw("text", Q.text, 603, IN, v + Vector2.new(z, z + 1), UN.label, false);
			end;
			if y.y > 40 then
				local l = Color3.fromHSV(UN._h, UN._s, UN._v);
				local y = Vector2.new(24, 14);
				local e = v + Vector2.new((s.x - z) - y.x, z + 1);
				T:_Draw("rect", Q.border1, 603, IN, e, y, true, 4);
				T:_Draw("rect", l, 604, IN, e + Vector2.new(1, 1), y - Vector2.new(2, 2), true, 4);
				local N = v + Vector2.new(z, (z + W.y) + 10);
				local t = Vector2.new(s.x - z * 2, 100);
				T:_Draw("rect", Q.border1, 602, IN, N, t, false);
				T:_Draw("rect", Q.body, 604, IN, N, t, true);
				local b = T:_GetMousePos();
				local S = t;
				local I = Vector2.new(S.x, 14);
				local h = N;
				local c = h + Vector2.new(0, S.y + 10);
				local K = c + Vector2.new(0, I.y + 10);
				local B = Vector2.new(S.x, 24);
				if T:_IsMouseWithinBounds(c, I) and P then
					local T = A(((b.x - c.x)) / math.max(1, I.x), 0, 1);
					if T ~= UN._h then
						UN._h = T;
						if UN.callback then
							UN.callback(Color3.fromHSV(UN._h, UN._s, UN._v));
						end;
					end;
					O = false;
				end;
				if T:_IsMouseWithinBounds(h, S) and P then
					local T = A(((b.x - h.x)) / math.max(1, S.x), 0, 1);
					local l = 1 - A(((b.y - h.y)) / math.max(1, S.y), 0, 1);
					if T ~= UN._s or l ~= UN._v then
						UN._s = T;
						UN._v = l;
						if UN.callback then
							UN.callback(Color3.fromHSV(UN._h, UN._s, UN._v));
						end;
					end;
					O = false;
				end;
				local u = Color3.fromHSV(UN._h, 1, 1);
				T:_Draw("gradient", nil, 605, IN, "horizontal", h, S, Color3.fromRGB(255, 255, 255), u);
				T:_Draw("gradient", nil, 606, IN, "vertical", h, S, {
					R = 0,
					G = 0,
					B = 0,
					A = 0,
				}, {
					R = 0,
					G = 0,
					B = 0,
					A = 1,
				});
				T:_Draw("gradient", nil, 606, IN, "horizontal", c, I, Color3.fromRGB(255, 0, 0), Color3.fromRGB(255, 255, 0), Color3.fromRGB(0, 255, 0), Color3.fromRGB(0, 255, 255), Color3.fromRGB(0, 0, 255), Color3.fromRGB(255, 0, 255), Color3.fromRGB(255, 0, 0));
				local Y = h + Vector2.new(S.x * UN._s, S.y * ((1 - UN._v)));
				T:_Draw("circle", Color3.fromRGB(255, 255, 255), 607, .9 * IN, Y, 5, false);
				local i = c.x + I.x * UN._h;
				T:_Draw("rect", Color3.fromRGB(255, 255, 255), 607, IN, Vector2.new(i - 2, c.y - 2), Vector2.new(4, I.y + 4), true);
				T:_Draw("rect", Color3.fromRGB(50, 50, 50), 608, IN, Vector2.new(i - 2, c.y - 2), Vector2.new(4, I.y + 4), false);
				local m = "#" .. string.format("%02X%02X%02X", math.floor(l.R * 255), math.floor(l.G * 255), math.floor(l.B * 255));
				T:_Draw("rect", Q.surface1, 605, IN, K, B, true, 4);
				T:_Draw("rect", Q.border1, 606, IN, K, B, false, 4);
				T:_Draw("text", Q.text, 607, IN, K + Vector2.new(B.x / 2, B.y / 2), m, false, "center", 12);
			end;
			if X and (not k and not T:_IsMouseWithinBounds(v, y)) then
				T:_RemoveColorpicker();
				k = true;
				X = false;
			end;
		end;
	end;
	if T._frame_tooltip then
		if not T._tooltip or T._tooltip.key ~= T._frame_tooltip.key then
			T._tooltip = {
					key = T._frame_tooltip.key,
					text = T._frame_tooltip.text,
					timer = 0,
					_anim = 0,
				};
		end;
	else
		T._tooltip = nil;
	end;
	if T._tooltip then
		local l = T._tooltip;
		l.timer = ((l.timer or 0)) + Y;
		l._anim = e(l._anim or 0, l.timer > .5 and 1 or 0, F(.28));
		local O = t(l._anim);
		if O > .01 then
			local s = T:_GetTextBounds(l.text);
			local v = s.x + z * 1.5;
			local W = s.y + z * .8;
			local y = T:_GetMousePos();
			local e = Vector2.new(y.x + 10, (y.y - W) - 6);
			T:_Draw("rect", Q.shadow, 999, (.18 * tN) * O, e + Vector2.new(1, 2), Vector2.new(v, W), true, 6);
			T:_Draw("rect", Q.surface0, 1000, tN * O, e, Vector2.new(v, W), true, 6);
			T:_Draw("rect", Q.border1, 1001, tN * O, e, Vector2.new(v, W), false, 6);
			T:_Draw("text", Q.text, 1001, tN * O, e + Vector2.new(z * .75, ((W - T._font_size)) / 2 + 1), l.text, false);
		end;
	end;
	if Z and (not k and (not T._menu_drag and T:_IsMouseWithinBounds(Vector2.new(H, E), Vector2.new(D, g)))) then
		local l = T._cached_mouse or Vector2.new();
		T._menu_drag = Vector2.new(l.x - T.x, l.y - T.y);
	end;
	for l, O in pairs(T._pool or {}) do
		for s = ((T._pIdx[l] or 0)) + 1, #O, 1 do
			local v = O[s];
			local W = T._pool_state[l][s];
			if W and W.Visible ~= false then
				pcall(function()
					v.Visible = false;
				end);
				W.Visible = false;
			end;
		end;
	end;
end;
return prodordie;
